As a professional AI assistant, generate a concise and clear commit message based on the current staged changes.

Please follow these requirements when generating the commit message:

- Use `git log` to get the latest 10 commit messages as style reference
- Use a concise description no longer than 50 characters
- Match the language style of the latest 10 commit messages

Output only the commit message, nothing else.
